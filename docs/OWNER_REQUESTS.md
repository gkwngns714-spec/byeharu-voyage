# OWNER REQUESTS — the open ledger

**Why this file exists.** On 2026-08-23 the owner said: *"last time you messed up not doing
everything i told you. Do not make the same mistake again."* They were right, and the cause is
structural, not attentional: a list of instructions held in a conversation is **lost at the next
context compaction**, and this project has already lost instructions that way twice — once producing
*"what i said before to make, nothing is done"*, once *"then all the sayings that i did, you didn't
make them? what else you didn't do?"*.

So the list lives here, in the repo, where it survives compaction and can be diffed.

**The rules for this file**

1. **Every instruction from the owner gets a row, in their own words**, the moment it is given.
   Paraphrasing is how an instruction quietly narrows. Quote them.
2. A row leaves `OPEN` only when the thing is **built AND verified in the running game** — not when
   an agent reports it done. An agent report is a claim, not proof.
3. A row that is **refused or deferred** does not get deleted. It gets a state and a reason, so the
   owner can overrule it. Silently dropping something is the failure this file exists to prevent.
4. `DONE` rows stay for one release, then move to the bottom under `SHIPPED`. The file is not a
   changelog — `docs/DEV_LOG.md` is — but a request must be visibly closed rather than vanish.
5. **An instruction that has to be given again is a bug report about this file.** It means the row
   was wrong, or it was closed without being verified. Diagnose the row and say what went wrong.
   **Do not tally how many times it was said** — the count is not the finding, and this file is a
   record of the work, not a score kept against the owner.

---

> **DEPLOY STATE, 2026-08-26 — PRODUCTION IS FULLY DEPLOYED.** Read every state in this file against
> this: `supabase migration list --linked` reads **52 of 52 applied, head 0058, nothing outstanding**,
> verified on the target on 2026-08-26. The site was already deployed and still is. **So for the
> first time the live client and the live database agree** — a row marked DONE here now means done
> against a database running the SAME rules the client expects, which was not true yesterday.
> The two blockers the previous note named are both gone: 0053's Postgres-17 failure was fixed
> (`ffbaf9c`), and 0057 landed after failing its first production push (DEV_LOG D27 — the lesson
> there is that green on PGlite AND green on CI's disposable Supabase still does not prove a
> migration will apply to production, because both boot empty and production does not).
> **Everything applied to production is now FROZEN and may never be edited** — 0051 through 0058
> included. `docs/RESUME.md` carries the same state.

---

## OPEN

| # | In their words | State | Notes |
|---|---|---|---|
| 1 | *"All game must be logged in so that they are safe."* / *"this is a online game, a user info and game progress should always be kept."* | **PART DONE — 2 settings left** | Cause found: `deploy-pages.yml` never passed `VITE_SUPABASE_*`, so the published site was the local-PGlite build — a private world per browser, no account. Workflow now REFUSES to publish without both secrets (no silent fallback). Needs: repo secrets added, Pages Source → GitHub Actions, and migrations 0025-0029 applied to the live project. |
| 2 | *"Where it pays more does not need to be given in buy."* | **DONE — verified** | Deleting the block and the whole `routes` thread through COMMAND, including the per-port fetch. Stays on MARKET. |
| 3 | *"when pressing hire, 12, it unfolds. this is uncomfortable. keep it without unfolding, and let me hire."* | **DONE — verified** | HIRE is one number; a fold in front of one number is a door in front of a doorway. |
| 4 | *"Provision also, does not need to be fold/unfoldable."* | **DONE — verified** | Same reasoning as #3. |
| 5 | *"and hands? seriously? change it like crew or something."* | **DONE — verified** | Client screens done (galley reads `crew 8/20`). Seven strings are SERVER-authored (refusals, the pirate report, the HIRE verb help) and are getting migration 0030 — a client gloss over served copy would be a second authority. "Hands are shaken" stays: that is the handshake idiom, not crew cant. Landed as 0030; HIRE now reads "Sign on crew from the idle men in port", read off the running game. Its own self-assert went RED first and refused to let the world open with the rename half-applied — Section 7C working. |
| 6 | *"when treading buy, i see a trade good with buy sell. i want to be able to click on buy and sell itself and do trades. when pressed unfold another so that i can choose how much i buy"* | **DONE — verified** | The price cells become the action and set the verb. Must go through the ONE order authority. Replaces `Choose <good>`. **Do not flatten this fold** — it is the opposite case from #3/#4. Measured by coordinator at 390px: 140 price-cell buttons (70 goods x buy/sell), min height 75.7px, 70 sell cells disabled and each SAYING "none aboard" on the cell; 0 `Choose` buttons remain. |
| 7 | *"create a 도감, separate tab, showing all the trade goods, ships, captains that are made in this game. categorize them, make filters"* | **DONE — verified** | Reference only, commands nothing. Also has to solve 8 nav tabs → 9 without an orphan row. |
| 8 | *"i want explain icon next to everything that has long sentences for explanation"* | **DONE — verified** | Also given earlier as *"too long explanation. this is a game, make it so."* — so the rule was written in `UI_DIRECTION.md` §4 and not applied everywhere. Refusal reasons stay visible. Sweep found the app already ~95% compliant (Explain is wired into the primitives); the real offenders are all in COMMAND, handed to the agent that owns it. |
| 9 | *"i want actual time shown on command, and how much left for it to change the prices live"* | **DONE — verified** | The read that serves the PRICES serves when they next move — deriving it client-side from two other served numbers would be a second authority for the cadence. Landed as 0029 (`world.market().clock`). Coordinator drove it: clock 11:24:40 to 11:24:44, countdown 5:19 to 5:15. **The re-ask at zero is also what STEPS the market** on any deployment without cron - never optimise it into a conditional fetch. |
| 10 | *"do the map layer and finish it"* | **DONE — verified** | Chart legibility and the act-from-map handoff both landed and were driven. Awaiting the final gate with everything else. |
| 11 | *"use fable also for better performance"* — AMENDED 2026-08-26: *"use different model since fable 5 is unavailable"* | **STANDING, AMENDED** |  The original rule was: every `Agent` call passes `model: 'fable'`; omitting it silently inherits the main-loop model, which is how it slipped. **That rule is suspended, by the owner's own instruction, because it became unfollowable.** On 2026-08-26 both dispatched agents died immediately with `You're out of usage credits… to keep using Fable 5`, and the owner said *"use different model since fable 5 is unavailable"*. So: **do not pass `model: 'fable'` while it returns a credits error** — omit the override and inherit the session model, which is what the 0059 and 0060 agents ran on. The original preference is NOT deleted: if Fable credits are restored, the performance reason for it still stands and this row returns to its first wording. The main-loop model remains the owner's to set via `/model`. |
| 13 | *"nope A, lets make it public"* (after first choosing B) | **DONE 2026-08-23** | Repo goes PUBLIC, not GitHub Pro. Verified safe on keys: `.env.local` never tracked, no key-shaped string anywhere in the repo or its history. **But `0001:145` seeds `world_secret` as a plain-text literal** (`voyage-v0-seed-6f2a91c4`) and `voyage.rng_raw` is public md5 in the same chain — publishing hands players a predictor for pirate attacks, haggle outcomes and the fair calendar. In git HISTORY too, so editing the file would not help. Fix in flight: production GENERATES its own seed at apply time so the repo never holds a real one. **Do not flip public until prod is proven to be running a seed that exists in no file.** Fix landed as **0031**: prod generates 128 hex chars of its own, and a CHECK constraint now refuses the published literal forever — the constraint, not the rotation, is what makes it permanent. Applied to prod 2026-08-23 and the assert ran INSIDE the production transaction: "rotated this apply: t", 128 hex chars, the published literal refused in 2 real rejected writes. Repo flipped PUBLIC only after that. |
| 12 | *"no spaghetti — separate independant codes, with plans for future … it has to be planned precisely and correctly"* | STANDING | Reactive half was already `docs/NO_SPAGHETTI.md` §§1-9 + `duplication.spec.ts`. The FORWARD half was missing and is now §7B. |

| 14 | Supabase access token stored on this machine | **DATED — expires ~2026-09-23** | Supabase no longer offers never-expiring personal tokens; 1 month is the max. Stored by `supabase login --token`, so no re-paste is needed until it lapses. **It was pasted into a chat transcript on 2026-08-23** - rotate it at the dashboard when convenient. When it expires, `db push` fails with a login error; that is the symptom, not a broken chain. |

| 15 | *"when pressing sail, stop folding the sail. when pressing buy stop folding buy... don't restruct anything"* | **DONE — verified** | Their first wording was *"the tab should be not unfolded. it should stay where it is"* and I read "not unfolded" as "collapse it". Backwards. **THE RULE: pressing a verb must not restructure the page AT ALL** - no collapse, no re-flow, no sticky sheet following the scroll. The six verb cards stay where they are and the chosen one is simply marked. Being reverted by DELETION, not behind a flag (NO_SPAGHETTI 5). This row is the ledger's own rule proving itself: a repeated instruction is a bug report about this file. **Closed 2026-08-23 after the root cause was found and measured.** OrderComposer derived a single open slot as `missingArgs(...)[0]` - the next unanswered question - so every answer on every verb unmounted the picker it had been given on. Deleted; FLAT_VERBS and the SAIL-dest special case dissolved into the general rule and went with it. Verified by the coordinator: 140 price cells before the tap, 140 after, filter present both times, scrollY 0. |
| 16 | *"how many crew to hire, have it + 10, +100, max, make it more friendly"* | **DONE — verified** | MAX must be the smallest of berths free, the port's idle crew, and what the purse can pay - never a ceiling the server would then refuse. A step past the ceiling clamps, never goes dead. |
| 17 | *"and how many crew? show my ship info regrading crew, how many needs, min amount, max amount etc at the right"* | **DONE — verified** | Crew aboard, minimum to sail, maximum berths, port's idle crew. "At the right" is the desktop rail; at 390px the rail is written FIRST and appears above - deliberate, keep it. |
| 18 | *"15 days of stores is not clear, 4.9kn is what? 56 t free? lying at lisbon is duplicate... make it separate tab for all of them"* | **DONE — verified** | A bare unit with no label is half of UI_DIRECTION rule 2. "lying at Lisbon" is printed TWICE - the fleet chip above already says it; DELETE, do not relabel. |
| 19 | *"right now the sail, all the port is alligned by sentence. make it like a cube, and what is yard?"* | **PART DONE** | Harbour TILES, not sentences. Distance stays the server's sailed nm. "yard" = shipyard, jargon; some instances are server-authored and would need a migration like 0030 did for "hands".. Client done everywhere (COMMAND, PORT, LEDGER all say shipyard). One SERVED string remains - the REPAIR verb help at 0021:297 - queued as its own wording migration, same shape as 0030 did for hands. |
| 20 | *"when pressing a location to sail, dont unfold, instead make the what sail needs bottom by comming out unfoldingly, also, shown in map where i clicked"* | **DONE — verified** | A harbour tile has nothing to unfold INTO - tapping it should just choose. Sheet from the bottom so the grid stays put. **The Issue control must never live inside a region that can clip it.** Chart must ring the TAPPED port - it was built for hover/keyboard, and a phone has no hover. **SUPERSEDED IN PART by row 45 (2026-08-24):** the owner's later spelling is that the confirm/Issue UNFOLDS beneath the tapped tile — the docked sheet stays dead, and "nothing to unfold into" no longer holds for the confirm itself. |
| 21 | *"the map in sail, i should be able to zoom, drag, move."* | **DONE — verified** | `SmallChart` now mounts the ONE gesture hook in an embedded posture (`useChartSurface` `scroll: 'page-vertical'`): drag pans, pinch zooms, and the Map tab's own +/−/find column rides the chart (`src/chart/ViewControls.tsx`, one component, both callers). The old inertness reason is ANSWERED, not ignored: no wheel listener is attached (the wheel scrolls the form) and `touch-pan-y` gives a one-finger vertical drag to the page — measured at 390×844: five finger-drags from the top of the open picker reach the Issue button 1,268 px below the fold, with the chart's viewBox byte-identical before and after. |
| 22 | *"i want you to categorize trade goods, common, uncommon, rare... make appropirate color or sign so that it is unique"* | **DONE — verified** | Rarity must be SERVED, authored or derived in ONE place - a client-side rule would drift between picker, compendium and market. Colour PLUS a non-colour channel, so it survives colourblindness and greyscale.. Landed as 0032, DERIVED from how many ports actually produce the good - not authored (a hand-set label would drift the moment the world grew from 12 to 214 ports, which already happened once) and not from price (every port trades every good here, so dear is not scarce - gold has 22 producers and is common; Venetian glassware has 1). exotic 6 / rare 20 / uncommon 22 / common 22. Every tier has a GLYPH as well as a colour; all four clear WCAG AA on the panel (6.06 to 9.00:1). **2026-08-25 — THE TIERS HAD STOPPED MEANING ANYTHING, AND MIGRATION 0051 FIXED IT.** 0032 derived rarity correctly and then cut it at three ABSOLUTE producer counts (≤2/≤5/≤12) calibrated for a 70-good catalogue. At 243 goods the catalogue read **exotic 133 (54.7%) / rare 58 / uncommon 27 / common 25** — the top tier was the DEFAULT, so *"exotic"* told the player nothing, which is the opposite of what this row asked for. 0051 makes the cuts fractions of the world's own mean producer count (`max(1, μ/4)` / `max(2, μ/2)` / `max(3, μ)`, μ = 5.391 today): **exotic 47 / rare 86 / uncommon 58 / common 52**, 171 goods re-tiered. Proven scale-free at k = 2/3/7/17/50 with 0032's old rule run as a positive control (at k=17 the old law puts 100% of the catalogue in one tier). **The colours and glyphs this row asked for are unchanged — only which goods wear them.** NOT YET ON PRODUCTION — corrected 2026-08-25: 0051 is pushed to `origin/main` and the client code is deployed, but production's database has not applied 0051 (`supabase migration list --linked` shows it with an empty remote column, and a Postgres-17 blocker in 0053 currently prevents deploying anything after 0050 — see `docs/RESUME.md`'s anchor). The live game still tiers the catalogue by 0032's old absolute cuts. |
| 23 | *"do all the work properly"* | STANDING | Do not silently narrow a request. If a slice is too big to do well, say which part to split out - half-doing five is worse than finishing three and naming the other two. |

| 24 | *"yes build set ratio but let me set it personally - adjust. Save it as preset. Give something like 6 presets"* + *"the ratio must account for number of crews as well"* + *"crew now, but show it clearly"* | **DONE — verified** | 0034. House-owned presets, cap 6 enforced by a TRIGGER on the table (so a future writer inherits it), a fleet holds a REFERENCE never a copy - proven by editing one preset and watching two fleets provision to the new figure with no re-apply. Fires on arrival only, inside voyage.settle's arrival arm, BEFORE the queue runs, so a queued onward SAIL departs provisioned. Sized from crew AT FIRE TIME: 5.04 t at 8 crew, 14.70 t at 20. Galley prints `keep Long haul - 30.0 days - at 8 crew`. |
| 25 | *"when pressing sail, stop folding the sail. when pressing buy stop folding buy... don't restruct anything"* | **DONE — verified** | Verb cards 6 to 6 to 6, identical pixel positions, scrollTop unchanged. Sticky sheet deleted. **Consequence measured and NOT hidden: Issue now sits 2,112px down, about 2.3 screens.** Offered a static fix (order line and Issue near the top, never moving); the owner answered 2026-08-24 with row 45 — for SAIL, Issue lives in the fold under the tapped tile; the head block keeps the line for every verb and the button for the non-SAIL ones. |
| 26 | *"what moves the price -> price movement... Why add unnecessary words?"* + *"stores last -> provision"* + *"cargo space or cargo, and 0T/54T - 0%"* | **DONE — verified** | Whole-screen label cut. Cargo now `4 / 60 t - 7% full`. A few fragments kept with reasons (`first ten at` cut to "cost" would leave the number ambiguous about what it prices). Fixed one figure carrying THREE names across the game - endurance / range / provision - now provision everywhere. |
| 27 | *"use worktrees from now on"* | **STANDING** | The rule was already in memory and I ran six agents in one clone anyway, which twice let a half-written migration take down every other agent's verification. Verified recipe recorded: `git worktree add` + an `mklink /J` junction for node_modules, so no npm install is needed. Every dispatch from now gets one. |

| 28 | *"i told you to not recreate anything when pressing a certain tab... how many times do i have to say this simple thing?"* | **DONE — verified** | **Said FOUR times, and the first two I built BACKWARDS.** Now measured, so it is no longer a guess: after tapping BUY there are **140 price cells and a filter box**; after tapping one price cell there are **0 and none** - the whole goods list is unmounted and replaced. THE RULE: a control SELECTS; it never replaces, collapses, re-flows or destroys the surface it was pressed on. Fixing at the MODEL level if the argument rows all swap this way, not per-verb. |
| 29 | *"what is max 12 in hire? just max is enough"* | **DONE — verified** | The figure is noise - the chip already means "as many as possible" and the number is on the rail beside it. |
| 30 | *"in provision, full and days, they does not have to be folded"* | **DONE — verified** | Same rule as 28. |
| 31 | *"find button on map, sail, it is not correct. have a symbol instead"* | **DONE — verified** | `+` and `-` are symbols, `find` is a word among them. Needs a glyph AND an accessible name - an icon nobody can name is worse than a word. One component serves both Map and SAIL. |
| 32 | *"when i press corfu or dubrovnik... the map does not move to that location. make it move - pinpoint"* | **DONE — verified** | The ring is drawn off-screen, so the chart says "marked" and shows nothing. **This is not a no-restructure violation** - only the viewBox moves; nothing appears, vanishes or resizes. |
| 33 | *"overall, check game itself - every aspect, and move simple words"* | **DONE — verified** | Whole-game plain-language sweep plus an audit. Served strings collected for a migration rather than glossed client-side. |
| 34 | *"make trade goods in blocks as well, not all alligned in sentences - horizontally"* | **DONE — verified** | "as well" = like the SAIL harbour tiles they liked. MARKET first; the tile goes somewhere SHARED because the command picker is certainly the second caller. |
| 35 | *"read again on top left of the game is useless. remove it"* | **DONE — verified** | Correct: the world re-reads every 30 s and on tab focus, so the button asks for what is already happening. Deleting it must NOT delete the screen-specific read behind it. |
| 36 | *"do the work properly. do the work all. do not leave anything behind. double check. always work with rules."* | STANDING | Every row above must be MEASURED, not glanced at, and "double check" means a second method - not the same look twice. Both agents told to say which items they measured and which they only eyeballed. |

| 37 | *"why are there only 70 trade goods? there should be thousand. real-life trading by regions + 대항해시대 오리진 + 대항해시대"*| IN PROGRESS — **stale label, flagged 2026-08-25**: the catalogue reached **243 goods** and it is in production (DEV_LOG D21/D23; migration 0041 carried +173 goods into the deployed world). Left at IN PROGRESS because rule 2 wants a drive, and because 243 is not the thousand that was asked for — the arithmetic for why is in the note below, and only the owner can close that. | Measured the collision first: 214 ports x ~6.5 offers = ~1,400 slots, so 1,000 goods averages 1.4 offering ports each. Works ONLY because a port OFFERS 4-9 but BUYS anything - exotics being single-source is the point. Agent must report the offer histogram and check the 0032 rarity tiers still mean something (a catalogue that is 80% exotic has no tiers). |
| 38 | *"each cities max 9 trade goods... Min 4, max 9. Add real islands... JEJU island might have korean gochujang"* | IN PROGRESS — **stale label, flagged 2026-08-25**: the ten island ports and the 4–9 rosters landed in migration **0041** and are in production (DEV_LOG D23). Not closed here because no drive is recorded (rule 2). | Tier already ranks city size (1:35, 2:79, 3:100 ports) - use it, do not invent a second axis. Islands need REAL coordinates and, critically, **reachability**: a port no sea route reaches is a port nobody can sail to. That is the most likely way this ships broken. |
| 39 | *"in map - make a small miniturized map at the corner of the map, that shows where my fleets are in color + symbol"* | **DONE — verified** | A minimap must not become a second chart - the chart layer has one entrance and one camera. Merged and driven by the coordinator. It draws NO port marks - measured, 30 of 35 ports sit under 5px apart at world span, which is a blob not an instrument - so it shows coastline, fleets and a viewport rectangle. 144x52px, 2.27% of a 390x844 screen, 0 labels underneath. Fleet states differ by SHAPE as well as colour, so they survive greyscale. |
| 40 | *"this game might need OSN system as well as other system built in byeharu, the previous game. audit and implement it so that later on, combat, exploration, npcs can be added"* | **AUDIT DONE · FOUNDATION BUILT · OSN REFUSED, with reasons** | Architecture, not a feature. `docs/PLATFORM.md` is the audit and the plan; migration **0035** is the whole of the code. **OSN is refused** and the owner said *"no OSN if it doesn't fit, do what's right"* — the five reasons are in PLATFORM.md §1, and the sharpest inverts the premise: in byeharu a fight is impossible without a `location_presence` row and free-coordinate arrival deliberately creates none, so OSN is the one state in which combat/exploration/NPCs *cannot* reach a fleet. Zones refused too (§2): the **leg** is already voyage's exposure surface. **What was missing and is now built:** *what can happen at sea* was stated in three functions and is now one catalogue with `voyage_events.kind` a foreign key into it — adding a kind is an INSERT. **Deliberately NOT built, with shapes written down (§6):** an actor/subject on an event, a second event per day, events ashore, per-sea weights, a verb registry. A column with no reader is byeharu's own four-item failure class. If the owner wants free-coordinate sailing as *gameplay* rather than as an import, §6 says what it would actually cost. |

| 41 | *"i don't want the fleet to ever touch land"* | **LAW — BUILT AND PROVEN (landed as 0046/0047)** | 2026-08-24: the law now holds BY CONSTRUCTION — a voyage exists only after `voyage.path_refusal` samples every segment of its course against the raster — and it is PROVEN non-vacuously on every run: proof 09 walks every stored course at sub-cell intervals through the one sampler and plants a straight Lisbon→Barcelona voyage that MUST be caught as E_LAND (it is, and the red is printed). The 41 silent approaches are fixed the other way round: the approach is real water inside every served figure (Bristol 65 nm, Hanoi 59, Suez 0 — the Gulf-of-Suez channel reaches its quay), costed and hazarded like all the rest. |

| 42 | *"it should go by sea without the fixed route - but fastest way possible. Also, in map, i should be able to pinpoint anywhere in the ocean to make a fleet move."*| **BUILT — LANDED as 0046/0047 (regenerated against main; 0048 reprices, 0049 retires the graph)** | 2026-08-24: ONE mover landed. The client PROPOSES a course (the pathfinder measured at 2–166 ms in JS against 302 s in plpgsql — NAVIGATION_PLAN §3), the server VERIFIES every segment against its own raster and MEASURES the distance itself. `voyage.route`/`route_direct`/`reach_from(graph)`/`sail_refusal(graph)` are DROPPED or re-cut in one migration — never joined. Any water point is a destination (`SAIL TO 33,-15`; the map tap snaps to sailable sea); she ANCHORS there and can always leave (round-trip stores gate). Lisboa→Nagasaki is served at 13,052 nm round the Cape, max latitude 38.7°N — the graph served 7,565 nm over the pole at 88.6°N. Divert turns WHERE SHE IS. The 782-leg graph, `data/sea-routes.json` and the lane layer are deleted. |

| 43 | *"sea is a free plane. auto sail is pathfind. wind and current will make the speed of ship differ - hence provision risk... encounters - npc will be randomly distrubutely, and then there will be a small tab on the map itself, showing the npc and its distance. i click on it to engage... different npcs in different areas of the sea - different levels... sometimes there will be attacks made to our fleet, sometimes natural disasters - that kills our crew, made our provision disappear, burn our cargo etc"* | **DESIGN SPEC — authoritative** | The owner answered the research questions themselves. Settled: free plane; auto-sail pathfinds; **wind and current change SPEED, and the point of that is provision risk** - a slow passage burns more stores, which is what makes free sailing tense rather than a formality; NPCs distributed by area with levels, surfaced as a **panel on the map listing nearby NPCs and their DISTANCE**, clicked to engage; the empty ocean is filled by consequence - attacks and disasters that take crew, stores and cargo. **THE HARD PROBLEM: `voyages.speed_profile` is frozen at departure and that freeze is what makes offline settlement byte-identical (proof 01).** Variable wind appears to break it and must not: wind has to be a pure function of (position, time, world secret) - the shape `voyage.rng` already has, `immutable` so Postgres forbids it reading the clock - so a voyage stays closed-form and any two evaluations agree to the digit. Losing that would mean the game only advances while someone is watching. **Also: if wind moves speed, the ETA quoted at departure is a FORECAST, not a promise** - UI_DIRECTION forbids printing a number the game will not honour. Note 0035 already made what-befalls-a-fleet a table with an FK, so the disasters are largely authoring kinds, not new machinery. **2026-08-24: the mover half is BUILT (rows 41/42). WIND IS DELIBERATELY LEFT OUT WHOLE, not half-built: the seam is ready (speed_profile freezes per segment; a segment has a bearing; the wind slice multiplies the per-segment speed inside voyage.depart as a pure function of (segment, season, world secret) and nothing else moves). NPC regions are the bv-seas slice's `seaOf`; `voyage.sea_at` is the declared seam and its interim body says so.** |

| 45 | *"when i want to move ship using sail, i want issue this order to be at the bottom of the location i click on it, unfolding it, but without making a new screen."* | **DONE — VERIFIED ON PRODUCTION 2026-08-25 BY DRIVING IT** | **The drive rule 2 asked for happened.** On 2026-08-25 the owner drove the LIVE production game and confirmed the chain end to end: tapping a harbour opens the port panel; `Send fleet` unfolds the fleet list **in place, with no new screen and without destroying the panel it was pressed on**; pressing a fleet unfolds the provisioning control. That is this row's instruction, verified where it counts. Built by commit `3ddcad1` on **2026-08-25** (`ArgPickers.tsx`, `CommandScreen.tsx`); the "BUILT 2026-08-24" this row used to carry pre-dated the code by a day. Supersedes the docked-sheet reading in row 20 and answers the open question in row 25. On COMMAND's SAIL, tapping a harbour tile still CHOOSES, and the order line + check + **the one Issue button** unfold after the ROW of tiles holding the chosen tile (the BUY good-row pattern) — tiles beside her hold still, only content below moves down. The head block keeps the forming line and LOSES its Issue/check while SAIL is chosen: one button per order. A destination pinpointed on the map (no tile) gets the same fold at the picker's foot. |
| 46 | *"in map, instead of send gaviota tab, i want to press send fleet, then it will unfold to my fleets, then when i press fleet, it will show how i can set my cargo/provision ratio. then i will press and it will send, without going to another screen or creating a new screen."* — first recorded that day in the shorter wording *"…then i will press and it will send, without going to another screen"* | **DONE — VERIFIED ON PRODUCTION 2026-08-25 BY DRIVING IT** (the ratio control itself is NEW today and is NOT yet driven) | **VERIFIED WHERE IT COUNTS.** On 2026-08-25 the owner drove the LIVE production game: tap a harbour → the port panel opens → **Send fleet** unfolds her fleets **in place**, no new screen, and **the panel it was pressed on is not destroyed** → pressing a fleet unfolds the provisioning control. That is the whole act this row asks for, and it is closed on a drive rather than on a report. **ONE PART IS NOT CLOSED AND IS SAID PLAINLY:** the ratio control inside the fold — the `15.0 / 16 days` hero figure, the Gauge, the two steppers and `Keep 16 & send` — **landed on 2026-08-25 AFTER that drive** (`src/features/map/SendFleet.tsx:507-547`), is on local `main` only, and **has never been driven on production**. It is built and green in a real browser at 390×844 against the real chain in PGlite, which is a test, not a drive. **This row was two rows until 2026-08-25** (a second #45 carried the shorter wording at IN PROGRESS while this one said BUILT — rule 5: the file was the bug). Folded to one row, one state, and the state is the conservative one: the code exists (`src/features/map/SendFleet.tsx`, 432 lines, added by commit `3ddcad1` on **2026-08-25** — the "BUILT 2026-08-24" this row used to carry pre-dated the code by a day), but rule 2 says built is not closed. `3ddcad1`'s own message claimed *"Driven end to end: the URL never changes at any step"*, and that was a commit message, which is a claim — **the row is closed by the owner's own drive of the production game on 2026-08-25, not by that message.** Supersedes the `Sail <fleet> here` button and its hand-off (row 10's flow). `SendFleet` (features/map): tap a place → **Send fleet** → her fleets unfold, each row dry-run against THIS destination (`cmd.preview` over the same proposed course the send carries — miles·days, or the refusal on the row, never silently dead) → press a fleet → her standing order (0034 presets, via the same `applyPreset` the FLEETS galley presses) unfolds beneath that row → press one and it SENDS (issue from quay/anchor, divert under way). Same draft, same issue path, no second composer, no preset editor on the map; the only remaining hand-off is a refusal's FIX that needs composing. |
| 47 | *"Gaivota carries 2.9 days of stores, and there is no chandler… - too long. make it very concise. This concise concept will have to be applied to all aspects of the game. Always show in graphics, concisely."* — recorded the same day in the wording *"too long. make it very concise. This concise concept will have to be applied to all aspects of the game. Always show in graphics, consiely"*, of this example: *"Gaivota carries 2.9 days of stores, and there is no chandler where she is bound - the round trip is 28.7 voyage-days; you need 33.0 - PROVISION first"* | **LAW — whole game. BUILT both halves (migration 0050, applied to production); NOT VERIFIED IN THE RUNNING GAME** | **This row was two rows until 2026-08-25** (a second #46 carried the same instruction as `LAW — whole game` — rule 5: the file was the bug); folded to one row with one state. **The migration named as the next slice below is 0050, and it is done**: `voyage.sail_refusal` returns `{code, sentence, figures}` as one value, `cmd.refuse` is the ONE raiser (figures via `PG_EXCEPTION_DETAIL`), `cmd.refusal_caught` is its inverse and replaces SIX hand-copied splits, `public.orders.error_figures` keeps a failed order's figures, and E_ENDURANCE's sentence now carries no digit at all. **Applied to production** — probed 2026-08-25 with the anon key: `orders.error_figures` answers `42501 permission denied` where an invented column answers `42703 does not exist`. It stays OPEN because rule 2 needs a drive in the running game, and none is recorded. The law itself is STANDING and applies to every future surface, not just refusals. THE LAW: a refusal is a bar, two figures and the fix; prose behind the ⓘ. ONE renderer now exists (`components/ui/RefusalNote.tsx`) and both refusal surfaces (map send flow, COMMAND's check) draw through it. **The client never parses a served sentence for numbers**, so the graphic face needs the server to SERVE the figures: `Refusal.figures {have, need, unit}` is already read off the envelope (lib/rpc/result.ts) and drawn when present; until the serving migration lands, the sentence stays visible (it is the refusal) — compact, code badge, fixes as buttons. ~~**Next slice (specified): a supersede migration — `voyage.sail_refusal` returns its figures beside the text (E_ENDURANCE `{have: stores-days, need: margin-days, unit: 'days'}`, E_DRAFT, E_CREW_SHORT…), `cmd.do_sail` raises them via `errdetail`, `cmd.issue`/`cmd.preview` read `PG_EXCEPTION_DETAIL` into the envelope's `figures`, and the sentences are cut to one clause in the same file (0030's shape).**~~ — **DELIVERED as migration 0050, 2026-08-25** (DEV_LOG D24). |

| 48 | *"i told you, min 4, max 10 trades goods per city. there should be a purpose to go to a city that is far away to get rare trade goods. i told you, capital cities - 10 items, mid sized cities - 4~8, small cities 4 , randomly distributed"* | **THE READING IS ANSWERED, AND 0061 IS BUILT BUT NOT DEPLOYED — this row stays OPEN under rule 2 until it is driven in the running game.** Migration `0061 a_city_sells_only_what_its_roster_names` (branch `osn-0061-city-trades-its-roster`, NOT merged, NOT pushed) takes the reading the words support: **a city SELLS only what its roster names.** `public.port_offers(port, good)` becomes the one authority — it READS `public.port_specialties` and derives nothing, so it survives the restoration of `data/ports.json`'s authored lists — `cmd.do_buy` refuses anything else with the **E_UNAVAILABLE it already raises for the culture mask**, and `world.market` serves the quay. **SELL is deliberately NOT restricted:** measured, a good is on the roster of only 5.30 harbours on average (3 goods on none at all), so a city that also refused to BUY outside its roster would leave a hold with ~5 buyers in a world of 224 harbours and strand cargo already afloat. `cmd.do_sell` and `public.port_goods` are therefore untouched, and the migration proves it by SELLING an un-offered good on a real fleet. **CORRECTION to this row's own disk figure, measured rather than assumed:** "1,288 pairs = 12.4 MB" is only true if the SNAPSHOT WRITER narrows, which is what 0061 does (`public.tick_price_snapshot` now samples offered pairs only: ceiling **~594.7 MiB -> ~14.1 MiB** at 0057's untouched 57-slot window). DELETING the `port_goods` rows instead would have raised `price_history_window()` to **2,430 slots** (`price_history_window_for(1288)`, measured) and put the ceiling straight back at ~600 MiB, because 0057's law is a budget and a budget-filling law fills its budget. Previously: **APPLIED TO PRODUCTION 2026-08-26 (verified: 52/52, head 0058) — but the READING of this row is OPEN and needs the owner.** 0058 built the roster as `public.port_specialties` (1,288 rows, ~5.75 per port), which sets what a city is GOOD AT. `public.port_goods` still carries all 243 goods at all 224 ports (54,432 rows), so **every city still OFFERS all 243 goods** and the market screen still lists them all. The words here — *"min 4, max 10 trades goods per city"*, *"capital cities - 10 items"* — read more like what a city SELLS than what it specialises in. **Do not close this row until the owner says which reading is meant.** It is not cosmetic: if a city should only TRADE its roster, `price_history` would sample 1,288 pairs instead of 54,432 (12.4 MB instead of 595 MB) and the project would fit Supabase's free 500 MB tier, which it cannot today (see `docs/RESUME.md`). Original build note follows. Green on BOTH engines (apply-proof run 32876450404: `pglite-gate` and `disposable-chain`), 17 guards broken on purpose and every one watched go red. **Measured, capital/mid/small: 5-9 -> exactly 10 (35 ports), 4-7 -> 4-8 (79), 4-5 -> exactly 4 (110).** The count is now a LAW (`public.roster_target_count`) rather than a hand-typed band, so it cannot drift the way 0041's did. **And the owner's POINT held, which was the risk:** rarity moved exotic 47->44, rare 86->75, uncommon 58->73 — further inside 0051's guardrails, not closer to the edge, so a far city is still worth sailing to. Authored pairings survived (Jeju's goods untouched). **CORRECTION to this row's first draft: the "14 ports offering nothing" were mine and they were wrong** — all 14 are `kind = 'SEA_PLACE'` ocean anchorages with no market by design since 0036; ZERO harbours were ever empty. The tier mapping is not a guess: `scripts/lib/world-derive.mjs:16` says tier 1/2/3 -> 5/3/2, "a great entrepot / a working port / a small harbour". Row stays OPEN until it is applied to production and DRIVEN there (rule 2). Originally recorded as: **OPEN — REPEATED INSTRUCTION, said 2026-08-25.** This is row 38 said AGAIN with harder numbers, and a repeat means the wrong thing was built (see the memory rule of that name): 0041 shipped a 4-9 roster by tier and the owner's rule is not 4-9 and is not one band for every city. **DELETE the 4-9 rule; do not adapt around it.** | THE RULE, exactly: **capital = 10 items; mid-sized = 4-8; small = 4; randomly distributed**, and the POINT of it is stated in the same breath - *"there should be a purpose to go to a city that is far away to get rare trade goods"*, so the roster is a means and rarity-at-distance is the end. Use the EXISTING `ports.size_tier` (row 38's note: "use it, do not invent a second axis"); the tier-to-city-size mapping is the one decision this needs and it must be written down, not assumed. Check it against 0051's rarity tiers afterwards: if every capital carries 10 and capitals are everywhere, exotic stops meaning exotic again - which is exactly the defect 0051 fixed and this row can undo. |
| 49 | *"i can't send a fleet in map. check why and tell me"* | **OPEN — BUG REPORT, under investigation 2026-08-31** | Reported live on production (head 0059) while the owner was playing. Not reproduced by any test: `tests/map.sendfleet.spec.ts:115` drives the ratio control against PGlite and passes, so whatever this is, the suite does not cover it. Three angles under investigation in parallel: the client path from press to RPC (`src/features/map/SendFleet.tsx`), every server-side SAIL refusal (`voyage.path_refusal`, `cmd.do_sail`), and a live browser reproduction with console and network open. **The owner asked to be TOLD why, so the deliverable is the diagnosis, not silently a fix.** |
| 50 | *"i need a real timer in map, telling me how long i've been on the sea, my provision depleting as the time goes, and my ship marker should be updated more frequently"* | **OPEN — three parts, none built** | Three distinct things and all three must land or none does: (a) **elapsed time at sea** shown on the map — the map today shows only the scale `1 min = 160 h sail` and no elapsed figure; (b) **provision depleting as time goes**, visible as it falls rather than only on the Fleets tab — this is the tension row 43 asked for and never got, and it pairs with the unbuilt wind slice (`wind_mult_v0` pinned at 1.00); (c) **the ship marker updated more frequently** — the map currently re-reads on a slow cadence (`read 0s ago` in the corner), so a fleet at sea appears to sit still. Constraint that must not be broken: the voyage is settled by a FROZEN speed profile and the read is the catch-up, so a faster marker must be a finer INTERPOLATION of the same authority, never a second mover. |
| 51 | *"when i press provision on map, it shouldn't go to a command page - no new page, but there should be provision settings on map as well, on the same page"* | **OPEN — being built in the same slice as row 49** | **This is rows 15/20/25/28/45/46 said again about one surviving hand-off, so by the repeated-instruction rule the thing to do is DELETE the hand-off, not adapt around it.** The offender is named in the code: `src/features/map/DetailPanel.tsx:64-67` declares `onCompose` as *"the hand-off to the one composer — reached ONLY by a refusal's fix that needs composing (PROVISION first, …)"*, and `SendFleet.tsx` calls it, which NAVIGATES to COMMAND. **The machinery to do it in place already exists on this very panel**: the KEEP & SEND fold is a provision control — it composes 0034's six presets and `cmd.provision_preset_*` — so provisioning on the map is not a new mechanism, it is the fold that is already there, reached from the refusal instead of from a send. Constraint: it must COMPOSE 0034's presets and must not mint a second way to set stores (row 46's rule, and NO_SPAGHETTI §7B). |
| 44 | (found by the coordinator, not asked for) **The world expansion cannot reach production** | **RESOLVED 2026-08-24: 0003 reverted to production bytes, growth landed as 0041, and scripts/db/world-guard.mjs fails any apply whose world is not data/*.json** | Migration **0003 was EDITED after it had run on production** (commit 1ae5ca5, "243 trade goods"). Prod applied 0003 when it carried **70 goods and 214 ports**; editing the file does not re-run it. So prod still holds the old world while local holds 243 goods and 224 ports, and **local looks perfect while prod is stale**. This is precisely what "never edit an applied migration" exists to prevent, and it was merged without being caught. Compounding it: `ports.json` carries 224 ports but the seed only writes **214** - the ten islands are in the data file and in NO migration, so they do not exist in any database, local or remote. **Fix: revert 0003 to what actually ran, and add a GROWTH migration that inserts the 173 new goods, the 10 islands and the rosters.** Both worlds then agree by construction. |

### A NOTE ON THE NUMBERING — repaired 2026-08-25

**This file broke its own rule 5 and it took a week to notice.** Two more rows were numbered **45**
and **46** here, and they were the SAME two instructions as rows 46 and 47 above, in the wordings
first written down: commit `4fcbdfc` (2026-08-24) appended them as `IN PROGRESS` / `LAW — whole
game`, and commit `3ddcad1` (2026-08-25) then wrote fresh rows for the same two instructions higher
up with build states, without folding the old ones away. **The result was two rows numbered 45 and
two numbered 46, one instruction carrying two contradictory states** — the map-send row said `BUILT
2026-08-24 — drive pending` in one place and `IN PROGRESS` in the other.

Repaired by folding, not renumbering: every number in this table is now unique, both wordings of each
instruction are preserved verbatim in the row that survived (rows 46 and 47), and each has ONE state.
Where the two states disagreed, **the more conservative one was taken** — rule 2 says a row leaves
OPEN only when built AND verified in the running game, and no browser drive of either is recorded in
this file or in `docs/DEV_LOG.md`.

What was carried across from the folded rows, and is not repeated above:

* **On the map send (row 46):** the whole act unfolds step by step — Send fleet → your fleets → that
  fleet's provision ratio → sent. **Nothing navigates, nothing is replaced.** Same good-row pattern:
  reveal detail under what you pressed, never rearrange what surrounds it. Must COMPOSE 0034's
  presets, not mint a second way to set a ratio, and must not become a second order composer.
* **On the concise law (row 47):** the shape wanted is a bar, the figures, the fix —
  `2.9 / 33 days [Provision]`. **This is `docs/UI_DIRECTION.md`'s existing rule — the number is the
  hero, prose behind the dot — applied to REFUSALS, which nobody exempted and nobody ever did.** The
  hard part was that refusal sentences are SERVER-authored, so either the server serves the FIGURES
  beside the sentence or the sentences shorten in a migration. **Never parse a served sentence for
  numbers.** (Migration 0050 did both.)

## KNOWN AND NOT YET FIXED — surfaced by the work, not asked for

These are stated rather than quietly carried. The owner may not care about any of them; they are
here so the choice is theirs.

| What | Why it is not done |
|---|---|
| ~~`BALANCE_MEDIAN_IN_BAND` (proof 05) is a lottery~~ **CLOSED 2026-08-25 — AND IT WAS HIDING A REAL DEFECT** | **THE LOTTERY IS DEAD, AND THE NUMBER IT WAS REPORTING WAS NEVER ABOUT THE ECONOMY.** The 0054 slice pinned the market on ONE authority (`proof.pin_market`, `scripts/db/market-fixture.mjs`, shared by proof 04, proof 05 and `tests/rpc.firstSession.spec.ts`, redrawing every row from the drift's own stationary law rather than removing it): five `db:proof` runs now agree to the digit. **And pinning it showed that *"a first voyage returns 12-18%"* — quoted in this repo's docs for a week — was a measurement of HOW MANY DRIFT TICKS THE HARNESS HAPPENED TO RUN.** A freshly applied chain has stepped the drift once on the 14,980 rows 0003 seeded and never on the 39,468 that 0041 added, so 72% of its prices sit at drift 0. **Every deployed world was paying ≈37.4%.** Worse, `BALANCE_DISTANCE_PAYS` had fallen to **18.87% long vs 16.19% short — 1.17x**, where the world's own geography is worth **3.31x**: the noise had all but erased the reason to leave home waters. Migration **0056** pulls `drift_sigma` **0.040 → 0.020**; geography recovers to **9.66 vs 5.88 = 1.64x** and the quay's offer shape moves with it (85 near routes fall to 55, 360 far ones rise to 405). The claim is now TWO markers: `BALANCE_MEDIAN_IN_BAND` 13.0-20.0, which says out loud it is a **regression tripwire** on the settled market, and `BALANCE_GRADIENT_IN_BAND`, which keeps the design's original 4-16 on a FLAT market — the economy the affinity knobs actually author, where it reads **7.0%** against the 7.5 they were tuned to. **The authored economy was already doing exactly what it was designed to do; everything above 7.5 was noise.** The historical diagnosis below is kept because it is how this was found. — **DIAGNOSED 2026-08-24, still open — its own slice.** The honest distances (landed as 0046/0047) lifted the medians to 14.6-17.8 across runs, straddling the 16 ceiling. The sanctioned lever was pulled: migration 0041 flattened the affinity gradient (-30% relative on the sweep's own statistic) — and the proof's median barely moved (15.9 / 17.3 after). The mechanism was then MEASURED, not argued: zero the drift and the same world's median drops to **8.8** — the statistic is a max over ~243 goods x ~8 ports of drift-noised gaps, so it is dominated by the drift amplitude, which no affinity knob touches. The proof also asserts a band over an ambient it does not own (the drift state at boot) — the repo's own proofs-own-their-preconditions rule points at the fix, but zeroing drift inside the proof exposed a second finding (route_scan_keep=3's shortlist drops the best trade at drift 0), so the two must land together, in a balance slice of their own. Every other marker of proof 05, and all seven other proofs, are green twice on the finished tree. |
| ~~`tests/rpc.firstSession.spec.ts` has the same root cause~~ **FIXED 2026-08-25** | Proof 04 was given a stationary-distribution fixture; this spec was not. The 0054 slice retired the forty lines of Box-Muller that lived inside proof 04 and made `proof.pin_market` the ONE fixture authority — proof 04, proof 05 and this spec all call it now, each with its own draw, so none can move another's numbers, and proof 04's market is byte-identical to the one it has been playing since 2026-08-23. |
| Proof 04 now winds the fair calendar | 0026's "the proofs run in a fair-free world" is half false; 0028's header says so. A fair can only make a trade cheaper, so it can only help the marker. The fixture was deliberately NOT reached into. |
| "Read this harbour's market" from the map | The read exists; the SEAM does not. "Which port is this house looking at" is component-local in MARKET and `sessionStorage` in PORT. A button that landed on the wrong port would be worse than no button. |
| ~~`world.reachable(fleet)` is not served~~ | RESOLVED BY 0039's model: under the free sea every water point is reachable and `world.reach(port)` serves the sailed distance to every place in one read — the endurance gate is the only per-fleet clause left, and `cmd.preview` answers it through the one authority. |
| `layout.spec` could pass while measuring NOTHING | **FIXED 2026-08-23.** Point `PLAYWRIGHT_BASE_URL` at a bare host and vite preview answers with its own "did you mean" page - no tables, no tiles, no skeletons - so `ready()` resolved and all 8 tests went green having measured a 404 helper. A non-vacuity guard now requires the screen to have rendered something; break-tested against a bare host and it fails with the reason. |
| ~~`db.chain` rebuild spec times out under parallel load~~ FIXED | Passes alone in 1.9 min; it rebuilds the whole world twice and gets slower with every migration added. At 34 it is close to the line. Needs its own timeout or a lighter fixture before it fails for real. |
| **NEW 2026-08-25 — migration 0055 (encounters) landed DARK, on purpose** | The sea now decides what befalls a fleet in it — derived from the danger tier and the piracy index the sea already carries, ten distinct mixes where there were three — but **nothing draws from it yet.** It was landed dark deliberately: another worktree was measuring what a first voyage returns, and moving the mix under that measurement would have invalidated it. **Lighting it is one migration and four statements, named in 0055's own header:** (1) `voyage.hazard_roll` becomes `voyage.encounter_at`'s body, which is written to be dropped in; (2) `update public.voyage_event_kinds set is_rolled = true, roll_weight = null where in_sea_mix` — which 0035's closure trigger forbids, so the same slice must ALSO drop `voyage_event_kinds_weights_close` and `roll_weight`, because once the mix is the draw a flat weight is a second authority; (3) `voyage.settle` gains five arms, one per new kind (FAIR_WIND subtracts delay hours, FOUL_WATER starts water over the side, SHOAL_WATER takes durability, DERELICT and CONSORT touch no ship); (4) the map panel gains the mix as a fifth fact per row. **Measured cost of lighting it: Barbary raid-days 43.0% → 20.4% of event-days, home waters 33.0% → 7.0%, Malacca 43.0% → 25.8%.** The frequency of event-days does not move at all — it changes WHICH thing happens, never how often something does. **This is the owner's call, and nobody has taken it.** |
| **NEW 2026-08-25 — a live economy knob moved: `drift_sigma` 0.040 → 0.020, and 0.020 was chosen over 0.015** | Migration **0056**. Recorded here because it changes what a real player earns, not a test figure. **The reason 0.015 was on the table:** it pays 12.5% (mid-band rather than at the ceiling), recovers geography further to **1.96x**, and keeps a fully bargained trader inside the DESIGNED 4-16 band where 0.020 does not (15.7 + proof 06's 2.7 = 18.4). **The reason 0.020 shipped instead:** 0.015 thins the near market hard — **39 short routes against 55 at 0.020 and 85 at the old setting — and that is the water a new captain starts in.** Both settings are measured through proof 05 itself, so a second step needs no new measurement. **RULE 1 FLAG, stated rather than hidden: the owner's exact wording of this ruling was not captured, so this row paraphrases a decision instead of quoting an instruction, which is the thing rule 1 exists to prevent.** If the wording matters, it is in the 2026-08-25 session and not in this repo. |
| **NEW 2026-08-25 — 40 harbours still snap over 20 nm to sailable water** | 0052 fixed Bristol (64.55 nm to Lyme Bay, across Devon, granted to her courses as a ~90 nm land-exempt corridor that the pathfinder USED). **She was not the only one and not even the worst.** Counted off 0052's own `sea_reaches` rows: **40 harbours snap more than 20 nm, 13 of them more than 30.** Largest: **Longyearbyen 67.68, Hanoi 58.68, Khambhat 57.77, Tokyo 47.69**, then Patras 47.21, Trondheim 45.98, Izmir 40.29, Amsterdam 35.47. Each is the same class of breach of row 41's law, and the fix for each is the same shape 0052 used — a CHANNELS entry for the water that is really there. **The census is not written down anywhere:** the generator prints it to the console (`scripts/build-sea-migration.mjs:298`) and no file keeps it. |
| **NEW 2026-08-25 — `Strait of Gibraltar`'s label is still not tappable, and the honest fix needs a refactor** | The harbour hit-target was 22 px while the centre of a harbour's own printed name sits **24.8-31.0 px** from its mark, so tapping the word you can read selected open sea. `GLYPH.hitRadius` is now DERIVED (9 + 22 + 7 = **38 px**) and every ordinary name is inside it. **A long name is not:** `Strait of Gibraltar` measured 119.6 px wide, so its far end is **129 px** from the mark. **A bigger radius is the wrong fix** — the right one is hit-testing the label's own BOX, and that needs the label PLAN lifted out of `ChartCanvas` (it is currently computed there against the selection and the chrome). Recomputing the plan at tap time would be a SECOND author of where a name is, and the two could disagree about which names were even printed. One authority, one radius, until the plan moves. |
| **NEW 2026-08-25 — `public.good_rarity` is now the biggest single cost in the market read** | **87 ms of the ~240 ms that remain** after 0053 took `world.market(Bordeaux)` from 1,442 ms to 241. It got there honestly — 0051 made rarity derive the world's own scale, and 0032's callers ask it once per good. 0053 named it and deliberately did not fix it: 0053's brief was the %NBR regression, and 0051 was one day old. A real cost, a different slice, written down so nobody measures it twice. |
| **NEW 2026-08-25 — the pre-built world image is proven in Node, not in a browser** | Cold boot went **171.7 s → 7.1 s** by shipping `dist/db/world-<fingerprint>.tar.gz` instead of replaying the chain in the player's tab (+4.75 MB, fetched only when a world must be built). It is guarded properly — generated never committed, named by the chain's fingerprint, carrying that fingerprint internally, refused on mismatch, and certified by `world-guard`'s existing authority against the database RESTORED FROM THE TARBALL. **But `tests/db.image.spec.ts:5` says of itself: *"PURE UNIT SPEC. No `page` fixture, so Playwright runs it as a plain Node process."*** The PostgreSQL and the tarball are real; **no browser has been driven through a cold boot off the image.** What is not recorded is anybody arriving at a live purse in 7.1 s. |
| ~~Nav bar geometry at 9 tabs~~ **ANSWERED 2026-08-25 — GROUPED, NOT A SECOND ROW** | This row said *"noted here in case the answer is to group tabs rather than add a row"*, and the answer is to group. **MEASURED at 390×844 before: nine cells of 130×56 in a bar 168 px tall — three rows, 19.9% of the screen spent on navigation. After: six cells of 65×56 in a bar 56 px tall, one row.** The game gained 112 px. The five that stay direct are THE VOYAGE — Command, Fleets, Port, Market, Map, the loop touched many times a session, and `UI_DIRECTION.md §3a` says depth added to a frequent act is a defect. The four behind **CABIN** are Ledger, Rank, Codex, Profile, and what they share is not "leftovers": **none of them acts on the world.** ONE TABLE STILL — `group` is a field on the tab in `ALL_TABS` (`src/app/navTabs.ts:86-98`) and `NAV_CELLS` is DERIVED from it, so there is no second array of members to drift. **The group REVEALS, it does not replace the bar:** the panel is positioned out of flow above the rail, so not one cell moves and the screen behind is overlaid rather than pushed — the owner's rule, said four times. `tests/nav.geometry.spec.ts` pins it. **ON PRODUCTION as of 2026-08-25** — corrected: this is client-only work (no schema involved), it shipped with the `15bd8c3` push, and the owner drove the live site and saw it. |

---

## SHIPPED

Moved here once verified in the running game. See `docs/DEV_LOG.md` for what each one cost and what
it taught.

| In their words | Where |
|---|---|
| *"make icon for each trade good"* | D18 — 70 goods, 70 distinct glyphs, nine redrawn after failing at 22px |
| *"click a trade good then unfolding"* | D18 — asked twice before it was built |
| *"내 주방 separate tab but next the dishes"* | D18 — the galley face on FLEETS |
| *"do the migrations - price history, player row, officers, skills"* | 0013-0016 |
| *"yes add haggling mechanic"* | 0022, retuned in 0024 after shipping as a 0.36% rounding error |
| *"This game fleet will be comprised with 8 ships"* | 0021 — and it exposed three caps that no rule read |
| *"do 8, 9, 10"* (captains, buffs, the inert three) | 0025-0027 |
