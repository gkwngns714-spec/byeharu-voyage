# The V0 chain

**Fifty-four** migrations (0001-0037, 0040/0041, 0045-0053, 0055-0059, 0061; the gaps 0038/0039, 0042-0044
and **0054** are deliberate - versions are arbitrated by `npm run db:check-versions`, never by
counting: the free-sea mover was cut as 0038/0039 in the helm worktree and LANDED regenerated as
0046/0047, and the slice that took the number 0054 on 2026-08-25 turned out to need **no migration at
all** - it moved the balance proof onto a fixture (`scripts/db/market-fixture.mjs`) and changed no
schema, so the number is spent and no file carries it. 0060 is being authored
concurrently in another worktree and is not here. Counting the files gives 54; counting the
numbers gives 61, and only the first is the chain). Each one
establishes **one concept**, and each one **proves its own effect in the same transaction that
applies it** — see `README.md` §3 for the self-assert rules this chain follows. Since 0041, every
`db:apply`/`db:proof` additionally runs the **world guard** (`scripts/db/world-guard.mjs`): the
applied world must EQUAL `data/*.json`, or the run fails — the standing repair for the day 0003
was found edited after production had applied it.

Every migration also re-asserts the grant lockdown of 0001 through the single authority
`public.client_write_grants()`, because a new table is exactly where the predecessor's
`GRANT ALL ON TABLES TO anon` came back. From 0018 there is a second authority to call beside it,
`public.client_executable_writers()`, because a table grant is not the only way in: a
`SECURITY DEFINER` function runs as its DEFINER, so a client role that can EXECUTE one bypasses RLS
without ever touching a table ACL.

Run it locally, in about ten seconds, with no Docker:

```
npm run db:check-versions   # no two files share a 14-digit version (a duplicate deploys as a no-op)
npm run db:apply            # apply the whole chain to real PostgreSQL 18 (PGlite) and print every receipt
npm run db:proof            # apply, then run scripts/db/proofs/*.sql and check every declared PASS marker
```

---

| # | File | What it establishes | What it self-asserts |
|---|---|---|---|
| **0001** | `the_world_is_read_only_to_everyone_but_the_server` | The `world`/`cmd`/`voyage` schemas; gap-filling shims for `anon`/`authenticated`/`service_role`/`auth.uid()` so the identical SQL runs on Supabase and on PGlite; `world_config` (36 knobs, every one described, **server-only** because it holds `world_secret`); the one knob reader `wc()`; and THE LOCKDOWN — every write revoked from the client roles on tables, sequences and functions in all four schemas, plus **this role's** DEFAULT PRIVILEGES retuned so nothing it creates later inherits one (§5a; §5b explains why the platform's own defaults are deliberately left alone). Mints a second authority, `public.objects_not_owned_by()`. Mints `public.client_write_grants()`, the one authority for "does a client role hold a write?". | `client_write_grants()` is empty — **after** a deliberately-granted probe table proves the query can find one (2 grants found, then revoked). The same zero independently through `information_schema.role_table_grants`. No default ACL grants a client a write or an execute. `world_config` has RLS on and **no** privileges for any client role. All 36 knobs described and readable; `wc()` **raises** on an unknown key rather than inventing a default. `gen_random_uuid()` and `auth.uid()` resolve. |
| **0002** | `the_static_world_exists` | `nations, seas, regions, ports, legs, goods, ship_classes` with RLS and read-only access for signed-in players. `legs` stores each edge **once**, canonically ordered, `UNIQUE` on the unordered pair. And `voyage.gc_distance_nm()` — the haversine of §B.3, IMMUTABLE, the one distance authority. | All seven tables exist, RLS is on all seven, each has **exactly one** SELECT policy, `authenticated` holds SELECT on all seven, lockdown still zero. `gc_distance_nm` reproduces DESIGN §B.3's own published figures: Lisboa–Cádiz **188.40 nm** and Ceuta–Tunis **750.75 nm**; zero on identity; symmetric. The second figure is a negative control — one number could be a stuck constant, two cannot. |
| **0003** | `twelve_ports_and_the_water_between_them` | The §K.1 world, seeded: 4 nations, 8 seas, 4 regions, **12 ports** at §B.2's exact coordinates, **22 legs**, **12 goods**, **3 ship classes**. Leg distances are the haversine times an authored detour factor where the sailed route rounds land; the factor and its reason are written into each leg's `notes`. | Counts against §K.1 (12/22/12/3). Every leg's endpoints resolve. Canonical ordering with no duplicate unordered pair. **No leg is shorter than the great-circle** between its ports, and the worst detour is ×1.604 (Cádiz→Sevilla, up the Guadalquivir) — a positive control, since a worst ratio of 1.000 would mean the detour data never landed. All **five** distances §B.3 publishes match exactly. The leg graph is **connected**: all 12 ports reachable from Lisboa. Every nation's capital resolves. The culture mask both **blocks** (wine refused at 2 Maghrebi ports) and **permits** (open at 10 Latin ones). |
| **0004** | `a_house_its_fleets_and_an_honest_ledger` | `players, fleets, ships` + the append-only `events` and `ledger`. `public.credit()` is the ONE money mover; `emit_event()` the ONE event writer; `current_player_id()` the ONE auth→player translation every RLS policy calls. The purse invariant `Σ ledger.ducats_delta = players.ducats` is a pair of **deferrable constraint triggers** over one check function — not a job that hopes. Structural rules: a composite FK makes a crossed ship/fleet owner impossible, a partial unique index allows at most one flagship, `ducats >= 0` is a CHECK. | Inside a rolled-back probe: `new_house()` opens with 8,000 ducats reconciled on both sides, and **five positive controls all bite** — an unbacked purse write, an UPDATE on the ledger, a DELETE on events, a second flagship, and an overdraft are each REJECTED. The probe then rolls back to zero rows, which is itself asserted. |
| **0005** | `every_price_is_derived_never_stored` | `port_goods` (144 rows: 12 ports × 12 goods of authored affinity, with `stock_target` and `production_rate` **derived** from size and affinity so two numbers cannot disagree), `trade_daily` for the §G.7.1 cap, and the §G.1 price: `world.mid_price(port, good, stock)` — taking stock as a parameter so one function serves both the spot price and every step of a large order — plus `world.spread`, `world.tax_rate`, `world.price`, and `world.quote()`, the stepped 10-tun execution that IS the price impact and also handles limit-order partial fills. | 144 rows; all 144 prices positive, inside the §G.7.3 band, `ask > bid`. **Price impact is real**: 200 tuns average 7.30 d. against 10 tuns at 6.95. **§G.7.4 holds on all 144 pairs**: a same-port round trip loses money everywhere, with the probe asserted to have examined 144 pairs. The §K.1 gradient is real: 60 sal costs 422 d. at Lisboa and fetches 795 d. at Cádiz. A limit fills partially; an impossible limit fills zero. |
| **0006** | `a_voyage_is_a_pure_function_of_time` | `voyages` (with the frozen `speed_profile`) and `voyage_events` keyed `(voyage_id, day_index)`. Routing (`route_direct` + `route` composing hard VIA waypoints), `fleet_speed` (slowest ship then formation), `endurance_days`, `depart()`, and the closed-form `progress_nm`/`position`. `voyage.rng_raw()` is **IMMUTABLE**, so PostgreSQL itself forbids it from reading the clock or a table; the secret is passed in. Delays from CALM/STORM shift the schedule via `delay_before_day()`, and day boundaries are **clamped at the ETA**. | `rng_raw` is structurally IMMUTABLE, repeatable, and varies with day, stream and secret; 2,000 samples inside [0,1) with mean 0.4919. The router finds Lisboa→Tunis, honours a VIA waypoint, and returns NULL off-graph. The starter Barca sails 188 nm at 4.91 kn = **1.59 voyage-days / 4.78 real minutes** (§K.1 quotes 1.6 and 4.7). Progress is 0 at departure, monotonic, pinned at `total_nm` from the ETA on. A 24 sim-hour CALM moves the ETA by **exactly 180 real seconds**. Duplicate `(voyage_id, day_index)` and a second SAILING voyage are both REJECTED. |
| **0007** | `a_fleet_arrives_and_the_queue_runs_itself` | `orders`, the six executable V0 verbs (SAIL, BUY, SELL, PROVISION, HIRE, REPAIR), `cmd.resolve_qty()` (ALL/HALF/n% read at **execution** time), `cmd.advance()` with the §F.3 **halt rule**, `voyage.report_line()` for §E.6 prose, and `voyage.settle()` — the idempotent catch-up whose `resolved_at` is the deterministic day boundary, never `now()`. | The §K.1 session runs as a 4-order queue **with no tick**: bought 50 sal, sailed 188 nm, settled 5 checkpoints lazily, sold, sailed home, docked at Lisboa with 8,275 d. against 8,000. Four more `settle()` calls change nothing. Stores fell and 5 wage payments landed. All six verbs take effect. An impossible order fails with a code and leaves the next order **pending**, not skipped. |
| **0008** | `one_line_of_words_is_the_only_way_in` | `cmd.fold()` (one place where `cadiz` = `Cádiz`), `resolve_port/good/fleet` (prefix-unique, the only source of `E_NO_SUCH_*`/`E_AMBIGUOUS`), `cmd.parse()` — the one grammar — `cmd.verb_schema()` for the tap-builder, `cmd.fixes()` for §F.5's "→ do this instead", `cmd.cancel_at`/`cmd.clear`, and **`cmd.issue()`, the only mutating entry point in the game**. `cmd.preview()` runs the **real executor** in a subtransaction and discards it, so the dry run and the commit share one code path by construction. | `verb_schema` serves exactly the 8 V0 verbs. `cadiz`/`CADIZ`/`Cádiz`/`CAD` all resolve to one port; `8_000` = `8,000` = 8000. `"s"` raises `E_AMBIGUOUS` **naming Safi and Sevilla**; `"zzz"` raises `E_NO_SUCH_PORT`. Noise words are optional. **A preview of `BUY sal 40` estimates 280 d. and leaves the purse and the stock untouched.** `E_STALE`; `E_QUEUE_FULL` at exactly the configured 12; a refusal carries code + sentence + 2 fixes; CANCEL/CLEAR empty the queue with the voyage still at sea. |
| **0009** | `the_world_reads_back` | `world.snapshot()`, `world.market()`, `world.fleets()`, `world.ledger()` — and `world.pct_of_neighbours()`, the §E.4 `%NBR` defined once over ports within 600 nm. Every fleet read calls `voyage.settle()` first: **the read is the catch-up**. `snapshot()` serves an explicit allow-list of knobs, never `world_config` wholesale. | Snapshot carries 12 ports / 22 legs / 12 goods / 3 classes / 8 verbs — and **does not contain the world secret**, asserted by searching the serialised payload for the secret's VALUE, not by trusting a list of key names. `market(Lisboa)` prices all 12 and marks wine UNAVAILABLE at Tunis and available at Lisboa. Salt reads `%NBR` **50.3 at Lisboa and 120.6 at Cádiz** — the §K.1 gradient. `world.fleets()` **alone** settles a 9-hour-stale voyage and reports the fleet docked at Cádiz with no tick having run. |
| **0010** | `the_clock_ticks_for_everyone` | `tick_arrivals` (keyed by `(voyage_id, day_index)`), `tick_market_drift` (keyed by a **drift slot**, so a retried cron run cannot walk the market twice; stock regenerates in **closed form** over elapsed game-days), and `tick_reconcile` (read-only; asserts the purse invariant, the SAILING invariant and the lockdown). Scheduled through pg_cron where the platform has it, reported honestly where it does not. | `tick_arrivals` docks a fleet unattended and a second call touches 0 fleets. `tick_market_drift` steps all 144 rows once per slot and **0 rows on a repeat call in the same slot** — both directions, since idempotence claimed one way is half a claim. After 60 forced slots, 0 rows sit outside the §G.1 clamp. Stock regenerates 263 → 1050 toward a target of 1050 with 0 overshoots. `tick_reconcile` passes on a healthy world **and RAISES on a purse falsified by 4,242 ducats**. |
| **0011** | `a_captain_signs_the_book` | `cmd.found_house(name, nation)` — the ONE way a signed-in player gets a house. Takes NO uid: it reads `auth.uid()`, so a caller can only ever found their own. | anon may not execute it and `authenticated` may; an unsigned caller is refused `E_NOT_SIGNED_IN`; a captain opens with the K.1 purse and 1 ship at LIS; and four positive controls BITE — a second house, a taken name, a 1-character name and an unknown nation. |
| **0012** | `the_clock_is_wound` | **When** the ticks run. 0010 owns what a tick DOES; this owns only its cadence, and derives it from `drift_slot_seconds` through `tick_cron_expression()` rather than restating a crontab. | The crontab and the knob agree; a 30-second and a 35-minute slot are both REFUSED rather than rounded; under PGlite it applies cleanly, schedules nothing, and says so. |
| **0013** | `a_price_remembers_what_it_was` | `public.price_history`, keyed `(port, good, slot)` so a retried run is a no-op **by construction**; `public.drift_slot_of()` as the named slot authority; `tick_price_snapshot()` (samples + prunes); and `world.price_history(port, slots)` — ONE call per port, keyed by good CODE, because a market screen draws a line per row. Derivation stays the only way a price is COMPUTED; this only records what it said. | `drift_slot_of()` returns the slot `tick_market_drift()` itself reports — the whole reason a second named authority is allowed to exist. A snapshot writes one row per market pair (**14,980**) and a repeat call in the same slot writes **0**. The recorded mid IS `world.mid_price` of the recorded stock. A row aged past the 288-slot window is **pruned**, proven by ageing one on purpose. |
| **0014** | `a_house_reads_its_own_name` | `world.player()` — the player row, at last, taking **no id** (identity is the JWT's). And `public.player_fame()`: fame **DERIVED** from the append-only record, never a counter, because 0007's verbs are deployed and because a stored total can drift from the ledger it is computed from. Weights are knobs. | A signed-in account with no house reads back `{"player": null}` — a STATE, not a refusal. A founded house reads its own name, nation, purse, counts and port. Fame opens at **0**, and a real purchase of 5,310 d. turnover scores **exactly 53** trade fame at 100 d./point — the stated amount, not merely non-zero — with exploration still 0 because nothing has arrived anywhere. |
| **0015** | `an_officer_signs_on` | `officers` + `player_officers` (the posting lives on the OFFICER's row — SECTIONS.md forbids officer columns on ships/fleets), `fleet_officer_bonus()` as the one reading, `cmd.hire_officer` / `cmd.post_officer`, `world.officers()`. **SUPERSEDES `voyage.fleet_speed()`** so a navigator actually makes the ship faster. | No officer column exists on ships or fleets. An unofficered fleet reads 0006's **exact** speed (4.9125 kn), so the supersede is a no-op without officers. A +8.00% navigator takes it to **5.3055 kn** — the stated amount — and the wage leaves the purse through `public.credit`. The per-fleet cap clamps the sum. Four refusals BITE. `world.officers()` reports `takes_effect` FALSE for the three specialties no rule reads. |
| **0016** | `a_captain_learns_a_trade` | `skills` + `player_skills` (never columns on `players`), `player_skill_bonus()`, `cmd.study_skill()`, `world.skills()`. A skill is **studied for money at a port with an academy** — not earned by XP, because XP means hooking 0007's deployed verbs, and because a derived skill would level itself and stop being a choice. **SUPERSEDES `voyage.endurance_days()`.** `ports.has_academy` finally means something. | At level 0 the endurance is 0006's exact figure, so the supersede is a no-op unstudied. Studying is REFUSED at sea (`E_AT_SEA`) and at a port with no academy (`E_NO_ACADEMY`), and allowed at one that keeps one — taking endurance from 15.000 to 15.900 d at +6.00%. The ceiling BITES (`E_SKILL_MAXED`) after studying to it; `E_NO_SUCH_SKILL` and `E_NOT_ENOUGH_DUCATS` bite too. |

| **0017** | `a_quartermaster_stows_the_hold_and_a_purser_shaves_the_spread` | Two of 0015's three inert specialties, wired. `public.ship_hold_capacity()` — the ONE answer to "how many tuns fit in this hull", the rated `ship_classes.hold` stretched by the fleet's QUARTERMASTERS — replaces the same subtraction hand-copied into four server functions (`fleet_free_hold` 0007:127, `fleet_load` 0007:249, `do_provision` 0007:579/583/609/613, `world.fleets` 0009:183), all of which now compose onto it. `world.quote()` gains ONE appended parameter, `p_fleet`, and a PURSER shaves the spread the quote executes at — `world.spread(port)` stays the port's own one-argument fact, and the three callers that know a fleet pass it. `world.fleets()` serves the STOWED capacity under `hold` (with `hold_rated` and `officer_pct` alongside) so the client's mirrored arithmetic stays true. SURGEON is deliberately left inert. And every function this file re-cut is taken off the client. | With no officer aboard a 60-tun hull reads **60** and the fleet's free hold **matches 0007's own definition recomputed inline**, so the supersede is a no-op; a **+6.00%** quartermaster takes the hull to **63** tuns and the free hold rises by exactly 3 — then `fleet_load` places **every** tun `fleet_free_hold` offered and REFUSES one more, so the check and the placement cannot disagree and no one pays for cargo that never lands. Naming an unpursered fleet reproduces the 0005 quote **to the digit** (ask 4002.70, bid 3674.15); a **+6.00%** purser moves them to **3999.70 / 3677.06** — the stated amounts, recomputed from `world.mid_price`, `world.tax_rate` and `world.spread` — with the bid still under the ask. The world cap **BITES**, on a cap the probe lowers to 5 itself rather than trusting the seeded 25 that two officers can never reach. SURGEON still reports `takes_effect` false, `specialties_read` names exactly three, and 0016's HAGGLING skill is still unread. Seven anon-executable SECURITY DEFINER functions are closed and the **17 still open chain-wide are NOTICEd by name on every apply** — see below. |

| **0018** | `only_a_signed_in_captain_may_move_anything` | The other half of the lockdown: the EXECUTE half. Every `SECURITY DEFINER` function in the four schemas is taken off `public`, `anon` and `authenticated` by a **loop over `pg_proc`** — the catalogue, never a list of names, because a list is correct on the day it is typed and stale the day someone adds a function. The eighteen the browser actually calls are granted back to `authenticated` **by name**, from `public.client_rpc_entry_points()`, which is minted here as the server's mirror of `src/lib/rpc/catalog.ts` and is the ONE place the sanctioned set is stated (the grants are a loop over it, and the new authority subtracts it, so granted and sanctioned cannot drift). `anon` gets nothing at all: every RPC is behind `RequireAuth` (`src/app/App.tsx:19`). And the root cause is fixed — 0001:240's `ALTER DEFAULT PRIVILEGES ... **IN SCHEMA** ... REVOKE EXECUTE` is re-issued **without** `IN SCHEMA`, the only form that records anything. Mints `public.client_executable_writers()`, the EXECUTE-side twin of `client_write_grants()`. | A probe `SECURITY DEFINER` writer created **after** the default-privilege fix is **born** unreachable by both client roles — the fix measured, not claimed — and then, granted to `anon` on purpose, is **found** by the new authority (1 row), and lost again when revoked (0). Then: **0** rows from `client_executable_writers()`; every one of the eighteen entry points named **individually** as still executable by `authenticated` and not by `anon` (counting to eighteen would not have said WHICH, and losing `cmd.issue` means no order can ever be given again); `anon` may execute **0** of the 94 definers and `authenticated` **exactly** those eighteen; `pg_default_acl` holds the schema-less row 0001 never wrote, granting a client nothing; and the database itself refuses `anon` with SQLSTATE **42501** on four representative writers **with USAGE on world/cmd/voyage deliberately granted first**, so what is measured is the function grant and not 0001's schema door — while `authenticated` still reaches `cmd.issue` and is turned away by the GAME (`E_NO_SUCH_FLEET`). |

| **0019** | `the_quay_names_the_port_that_pays` | **THE COMPARISON.** `world.trade_routes(from, fleet, max_legs, limit, to)` — for each good a port sells, the reachable port that pays most for it, priced end to end through the SAME `world.quote()` a committed BUY and SELL execute at, over the SAILED leg distance, and only to destinations this fleet may actually reach. Ranked by what the voyage PAYS (profit per sea-mile was measured and rejected: it put a 16 nm, 49-ducat hop above a 1,847-ducat one); the quantity it assumed and the bounds it searched under are in the payload. Three folds first, because ranking destinations needs answers that used to have two possible authors: `voyage.reach_from` (THE shortest path over the leg graph — 0006's Dijkstra, exposed at the shape it always computed, with `route_direct` re-cut to walk its predecessors); `voyage.sail_refusal` (THE answer to "may this fleet sail there?" — crew, flagship, draft, ice, endurance — which `cmd.do_sail` now raises from); and `world.market` computing %NBR ONCE per good instead of four times, behind a CTE fence, 1,303 ms → 331 ms. `neighbour_radius_nm`, `advice_buy_below` and `advice_sell_above` become knobs and `world.snapshot` serves them, because the client declared its own copies of 600/90/110. **%NBR itself is unchanged**: it is an honest LOCAL price index and it was never able to predict a profit — see the header. | `reach_from` settles all 213 ports from Lisboa with every predecessor a real leg, and BOTH bounds (nearest-N, stop-at) agree with the unbounded run on every port they return; 12 sampled `route_direct` paths are each a real chain of legs, match `reach_from` to the digit and are beaten by no shorter two-leg path; `world.market` serves all 70 goods with buy/sell/%NBR/advice UNCHANGED, recomputed inline from 0009's own definitions; `sail_refusal` and `cmd.do_sail` return the **IDENTICAL SENTENCE** for a port beyond endurance, and the quay excludes that very port; pinning the scan to one destination reproduces the open scan's rows for it exactly; and **the headline: the quay's own top row is PLAYED** — BUY here, SAIL there, SELL, through `cmd.issue` — and the orders move EXACTLY the predicted outlay and proceeds, to the ducat. Plus the positive control for why the file exists: a good that reads ABOVE the sell threshold at a port one leg away and LOSES money on the voyage. |

| **0020** | `the_verbs_speak_to_a_captain_not_to_a_schema` | `cmd.verb_schema()`'s eight `help` strings become player-facing copy. They are printed on the Command tab's verb cards now, and two of them were never written for that: SAIL said *"along the authored leg graph"* (a table name) and SELL said *"not when it is typed"* (an interface this game no longer has — orders are composed by tapping). Nothing else moves. | The grammar is **byte-identical** to 0008's — proven by capturing the LIVE schema before the replacement, stripping `help` from both sides and requiring equality, not by a sentence in the header; 8 of 8 verbs carry a sentence of at least 40 characters; a sweep of 14 developer words finds none of them in front of a player; and the re-cut kept its grants (`authenticated` yes, `anon` no, 0 client write grants, 0 client-executable writers). |
| **0021** | `a_fleet_of_eight_and_an_order_said_in_one_breath` | `ship_max` 4 → 8, and the three house caps become a rule instead of a sentence: `public.assert_house_caps()` **on the table as a trigger**, so a future shipyard verb inherits the cap rather than re-deriving it. Plus `cmd.verb_schema()` gains a served `note` beside `help`, so a verb card is one line and the fine print is still authored server-side. | DESIGN C.4's `fleet_ship_max`, unread by anything in twenty migrations, REFUSES a ninth hull in one fleet while the same hull is ACCEPTED into a second — a cap, not a blanket. B.3's 7+ formation band is exercised for the first time (4.6669 kn at eight hulls against 4.9125 at three). The grammar is BYTE-IDENTICAL to 0008's with both prose keys stripped, and **6 mechanics are proven to have MOVED from `help` into `note`** rather than been deleted. |
| **0022** | `a_bargain_is_struck_on_the_quay` | HAGGLING, and the ONE authority for what a house executes at: `world.spread_effective(port, good, fleet)` replaces the expression 0017 inlined in `world.quote`. `public.haggle_daily` is keyed like `trade_daily` so a new game-day is a new row and nothing expires anything. The roll is `voyage.rng` composed, never a second RNG, and **the attempt index is written before the draw**, so save-scumming is structurally impossible rather than discouraged. | The unhaggled economy is BYTE-IDENTICAL against a **pre-image of 120 real quotes** taken through the old `world.quote` before it was replaced. Every attempt of the day is PREDICTED from `voyage.rng` before it is played. Haggling moves the port's cut and never `world.price()`'s mid, ask or bid. The purser and the bargain **compose multiplicatively**, and the floor FIRES on a purser at the cap. |
| **0023** | `the_read_wall_is_standing_again` | The repair for a regression 0018 put on the live server: it revoked `public.current_player_id()` from `authenticated`, and **every RLS policy in the chain calls it**, so a plain client SELECT raised 42501 on 11 tables. Mints `public.caller_evaluated_functions()` — the third member of the family beside `client_write_grants()` and `client_executable_writers()` — which sweeps all five ways a function can be caller-evaluated and **refuses to grant anything VOLATILE**. | 11 gaps before, 0 after, by a LOOP over the new authority that names no function. As `authenticated`, all 11 repaired tables read, and on 9 of them one house sees EXACTLY its own rows and ZERO of another's — asserted per owner, so it holds on a live project. A CHECK built on purpose to be unreachable IS reported, so the zero is a finding and not a blind spot. |
| **0024** | `a_bargain_worth_striking` | Haggling retuned after it was driven in a browser and saved **19 ducats on 40 tuns**: step 0.15 → 0.25, cap 0.30 → 0.75, floor 0.55 → 0.20. Knobs only; not one line of the mechanic moves. | A bargain at the cap is worth **2.883 per cent of the stake** over a round trip against 1.153 before, measured across all 214 ports. The unhaggled economy is BYTE-IDENTICAL across another 120-quote pre-image. `3 × 0.25` lands EXACTLY on the cap, so no attempt of the day is wasted — and the CEILING for any setting of these knobs is stated (3.84 per cent of stake) rather than promised. |
| **0025** | `the_table_of_captains` | **RANK.** `world.standings(limit)` — the ONE place another house's figures cross the wire, serving a name, a nation, a standing and the fames it is computed from, and nothing else. `public.standings` RECORDS what `public.player_fame()` (0014) said, keyed `(board_slot, player_id)` like `price_history` (0013), so derivation stays the only way a standing is COMPUTED. `public.standings_slot_of()` is the named slot authority; `public.settle_standings()` is O(houses) once per slot and the READ is the catch-up (0009's idiom), so there is no cron dependency. **GRANTS** (one entry point). | Every board row's keys are EXACTLY the sanctioned nine, and a probe house's purse is ABSENT from the serialised payload — searched for the VALUE, not trusted from a list of key names — while `public.standings` refuses both client roles with 42501 and carries **no policy at all**, so 0023's read-own wall was not widened by one row. Every settled row equals `player_fame()` recomputed at read time. Ties are COMPETITION-ranked: two houses level on fame settle BOTH first with `tied` true and the third at position **3, not 2**. The first settle in a slot writes one row per house and the second writes **0**. Economy BYTE-IDENTICAL on a 120-quote pre-image. |
| **0026** | `the_fair_is_on_at_the_quay` | **BUFFS**, as `docs/SECTIONS.md` asked: `public.buff_kinds` (authored) + `public.active_buffs` (a row with a `starts_at` and an `ends_at`), read at the point of use by `public.buff_multiplier()` and folded into no stored total anywhere. ONE kind is authored and it is READ: a FAIR takes 30 per cent off the quay's PUBLISHED cut through `world.spread(port)` — so `world.market()`, `%NBR`, the price history and every executed trade see the same number, and `world.spread_effective` (0022) is not touched at all. The calendar is a pure function of `voyage.rng`; `public.tick_buff_calendar()` is the one writer, wound by pg_cron where there is one and by `world.buffs()` where there is not — **and, from 0028, by `world.fleets()`, so a fair no longer depends on one screen being opened.** **GRANTS** (one entry point). | Unlike a bargain, a fair moves the PUBLISHED price: the ask falls, the bid RISES, the mid does not move. `world.market()` prints the very number `world.spread()` answers, and a real 20-tun `cmd.do_buy` is cheaper by exactly that. TIMED in both directions — a fair that has not opened and one that has closed are each worth zero, while the SAME row inside its own window is worth the magnitude. Fair × purser × bargain compose multiplicatively; the cap BITES at 50 on two fairs summing to 60. The calendar is re-derived from `voyage.rng` in BOTH directions with 0 disagreements and a repeat tick writes 0. Economy BYTE-IDENTICAL on a 120-quote-and-spread pre-image. |
| **0027** | `the_surgeon_the_bookkeeper_and_the_navigators_apprentice` | The last three inert bonuses. SURGEON → `public.raid_crew_lost(fleet, crew, base_loss)`, composed by `voyage.settle` (**sliced, not retyped**) and spoken by `voyage.report_line`. ACCOUNTING → `world.daily_cap_remaining`. NAVIGATION → `voyage.fleet_speed`, **composed through the same single authority the NAVIGATOR officer already uses** rather than deferred a third time: 0016 deferred it to weather, and 0026 established that a timed modifier cannot honestly reach speed because `voyages.speed_profile` freezes it. `world.officers().specialties_read` and `world.skills().effects_read` now name all four of each. **Grants and revokes NOTHING.** | The SURGEON is proven on a REAL raid, replayed on the SAME deterministic roll with a surgeon signed on — and the first draft's own assert caught a defect worth keeping: shaving the LOSS FRACTION rounded a 7 per cent surgeon away entirely on an eight-hand Barca, so he works on the HANDS instead. NAVIGATION is proven not to double-count by FOUR measurements: the officer authority is blind to the skill, the skill authority is blind to the officer, the speed is EXACTLY `base × (1+nav) × (1+skill)`, and it is NOT the sum form. ACCOUNTING lifts the allowance by exactly `player_skill_bonus`, and the SAME order refused `E_DAILY_CAP` before the tuition goes through after it. Two pre-images — 120 quotes and 200 daily allowances — both zero drift. |
| **0028** | `a_fair_happens_because_the_game_is_played` | **THE CALENDAR IS WOUND BY PLAYING, AND THE OTHER CLOCK CROSSES THE WIRE.** Supersedes two reads and nothing else. `world.fleets()` — the read `AppShell` makes every 30 s and on every tab focus — now winds 0026's buff calendar, so a fair happens because somebody is playing rather than because somebody opened the PORT tab; before this, on any deployment without pg_cron, `world.buffs()` was the only thing that drew a fair ANYWHERE and `PortFair.tsx` was its only caller. `world.snapshot()` was measured and **rejected** as the home: the client calls it ONCE per session and caches it (`worldStore.ts:243`), it is `language sql STABLE` so PostgreSQL would refuse the write, and `world.fleets()` is already the catch-up read. **ONE writer still draws a fair** (`public.tick_buff_calendar`); this is a third CALLER beside pg_cron and `world.buffs()`, which is untouched. `world.snapshot().config` also gains `game_day_seconds` — DESIGN D.1's CALENDAR clock, which had never crossed the wire, so `duration_game_days` / `season_game_days` could only be printed on the VOYAGE clock (a different rate) or not at all, and 0026's panel correctly printed not at all. And `snapshot` gains a **`nations`** catalogue, because `standings().board[].nation` and `ports[].nation` were both bare codes with nothing on the wire to resolve them. | The read that runs every thirty seconds wound EXACTLY the set `voyage.rng` draws for the season — not "more than zero" — with **no house signed in and no quay opened**; a second read moved no row, the writer reported 0, and the row-id **digest** is unchanged, so it is a probe and not a rewrite (the count-only version of this assert passed a deliberate break and was strengthened); `world.buffs()` still winds through its own door; **1** function writes `active_buffs`, 3 read it, and the tick has exactly **2** callers, NAMED. Both re-cut functions are their previous definitions plus ONLY the lines marked `0028 WIND`, proven by stripping those lines out of `pg_get_functiondef` and matching a pre-image character for character; the snapshot payload is byte-identical jsonb outside the two additions and the world secret is still absent from it. THE SEAM: a FAIR's `duration_game_days` × the served `game_day_seconds` reproduces the real window the writer wrote **to the second**, while the voyage clock would have been wrong by a factor the receipt prints (×16) — the negative control for why the key exists. Every nation code the wire carries — every port's, every board row's — resolves in the catalogue, and `world.player().nation_name` still says the same word it does. |
| **0029** | `the_market_says_when_it_next_moves` | **THE PRICES' OWN CLOCK, AND THE READ THAT HONOURS IT.** Supersedes `world.market()` (0019:568) only. The payload gains ONE object, `clock` — `now` (this read's own instant) and `next_change_at` (the instant the drift walk next steps, from `public.next_drift_change_at()`, minted here as the named inverse of 0013's `drift_slot_of`) — and the function gains ONE opening statement: it **winds `public.tick_market_drift` before pricing** (0009's catch-up idiom), because on every deployment without pg_cron NOTHING else ever stepped the drift, and a countdown served without a mechanism reaches zero and lies. Deliberately NOT served: `slot_seconds` (inviting the client to multiply its way to the following boundary is the second authority this file exists to end — `world.price_history()` keeps its copy as the history-bucket width) and `priced_at` (no stored fact says when the drift last stepped; a field that cannot be served honestly is not served). Deliberately NOT wound: `tick_price_snapshot` (measured 2.3 s per call, 1.6–2.1 s even as a same-slot no-op — a read must not pay seconds for a record that is cron's) and `world.fleets()` (a settled-slot tick is 5–9 ms, 3–5× the entire 1.7 ms read 0028 defended; and unlike a fair's authored window, a skipped OU step is a legal quieter walk whose staleness is observable only through a price read — which now catches it up). Cost, measured interleaved in one session: the wind's steady state is inside the noise of the ~300 ms read; the once-per-slot step (208–353 ms) is paid by one read per slot, exactly the work cron schedules out of band. | The payload minus `clock` is byte-identical jsonb to a pre-image taken through the 0019 definition; the clock carries EXACTLY two keys and its `now` is the transaction's own instant. THE SEAM, on the tick's own behaviour and never a retyped formula: the tick steps **0** rows one second before the served instant and **all 14,980** at it. A market pushed one slot behind is caught up by ONE read — 0 rows left behind, the served ask equal to `world.price()` recomputed post-step to the digit — and a second read in the same slot serves the identical payload while `price_history` moves 0 rows. BOTH forbidden client guesses are measured wrong on a CONSTRUCTED mid-slot instant (never a lottery): `now + slot_seconds` misses the boundary by exactly 137 s, and with the cadence retuned 600 → 300 in a rolled-back probe the served answer follows the knob while the stale arithmetic misses by exactly 300 s. ONE function calls the tick (named: `world.market`), one reads the new authority, grants unmoved, 0 client write grants. Proof 04 pins `drift_slot` beside the drift it already pinned, so the wind cannot redraw its deterministic fixture. |
| **0030** | `the_crew_are_called_the_crew` | **THE OWNER'S WORD** ("and hands? seriously? change it like crew or something"). Supersedes `cmd.do_hire` (0007:632), `voyage.report_line` (0027:196) and `cmd.verb_schema` (0021:249) — **sliced, not retyped**: each live definition is read back from `pg_get_functiondef`, each hunk asserted to occur exactly once before it is replaced — plus ONE data row, ZAHRA's officer blurb (0015:350). Deliberately left alone: `cmd.haggle`'s "Hands are shaken." (the handshake idiom — a blind sweep would print "Crew are shaken", which is worse), and two source comments (`voyage.settle`, `public.raid_crew_lost`) no player is ever served. | Reverse-substituting the new words back reproduces each pre-image to the character, so nothing but the word moved in any body. A catalogue sweep proves the word now survives in EXACTLY three named places (idiom + two comments) — and that sweep **found `raid_crew_lost` on this file's first apply**, a carrier the authoring grep had missed, which is the probe proving it can see. The served surfaces are read back: the HIRE card, all three moods of the after-action prose, the roster payload (with ZAHRA's new sentence required PRESENT, so "no hands in the payload" cannot pass because blurbs stopped being served), and all three hire refusals provoked off the live verb. Grants unmoved. |
| **0031** | `a_secret_printed_in_a_book_is_not_a_secret` | **THE SEED LEAVES THE BOOK, BEFORE THE BOOK GOES PUBLIC.** 0001:145 seeded `world_secret` as a plain-text literal in this repository — and `voyage.rng_raw` (0006:113) is public md5 in the same chain, so anyone holding both could compute in advance which voyage-days are attacked, whether a haggle attempt wins before it is spent, and every fair's opening. Supersedes exactly ONE fact: the VALUE (and description) of that row. The secret is now GENERATED ON THE DATABASE at apply time — four core `gen_random_uuid()` draws (488 random bits as 128 hex characters; NOT pgcrypto, which PGlite measurably lacks, so one code path serves local, CI and Supabase) — and is rotated **only if the live value is compromised** (the published literal, or under 64 characters): rotating on every apply would re-dice a live world's unsettled days on every deploy. `world_config` gains the CHECK `world_config_secret_is_private`, so the database itself refuses the retired literal and any hand-typed short value, on every write, for ever — the half of the fix that cannot regress by someone pasting the old value back. Deliberately left alone: the fair calendar already drawn (world.buffs serves every wound window to any authenticated player, so a drawn season is in-game public information), and market drift (never secret-seeded, 0010:107). Nothing anywhere prints the value: every message reports lengths, counts and booleans. | The live seed is NOT the published literal, is 128 lowercase-hex characters with ≥12 distinct symbols, and BOTH rotation branches are proven non-vacuous (compromised ⇒ it moved; private ⇒ it did not). The refusal is proven by REJECTED WRITES — the literal and a hand-typed value both die on the constraint — and is not total: a legal rotation-shaped value goes through (discarded). THE CHEAT IS CLOSED on all three stream families ('hazard', 'haggle:…', 'buff:…'): `voyage.rng()` still equals `rng_raw(..., <stored secret>)` (the instrument's positive control) and no longer equals `rng_raw(..., <the literal>)`. The rotated value is absent from `world.snapshot()` BY VALUE, after position() proved it can find a key that IS served and the payload proved non-trivial. Grants, executable writers and world_config privileges all still zero. |
| **0032** | `a_good_is_as_rare_as_the_ports_that_make_it` | **EVERY GOOD SAYS HOW RARE IT IS.** A `rarity` tier — common / uncommon / rare / exotic — served on `snapshot.goods[]` and on every `world.market()` row. DERIVED, not authored, from the one editorial fact 0005 already prices from: how many ports produce the good (`port_specialties`). Scarcity of SOURCE, not price — every port trades every good, so gold (22 producers) is dear and common while glassware (1 producer) is mid-priced and exotic. Two functions: `public.rarity_from_producers(int)` is the immutable threshold law (≤2 exotic, ≤5 rare, ≤12 uncommon, else common — the only place the thresholds exist) and `public.good_rarity(uuid)` composes the count with the law; both are server-only, so the tier crosses the wire as a field and the formula never does. Supersedes `world.snapshot()` (0028) and `world.market()` (0029), each extracted by script and changed by exactly one line. Deliberately does NOT touch pricing: scarcity already reaches the price through 0005's affinity gradient, and wiring rarity in would push one fact through two doors. | The threshold law pinned at nine values probing both sides of every boundary. Every good tiers into one of the four; ALL FOUR tiers inhabited (measured on this world: exotic 6, rare 20, uncommon 22, common 22 of 70 — reported, not pinned) and no tier holds >60% of the catalogue. The scarcest good in the world tiers exotic and the most-produced common, by a deterministic pick. Snapshot serves the field on every good and the wire's word equals the authority's answer; so does every market row. BOTH supersedes are proven no-ops but for the field: the old payloads captured before the replace equal the new ones stripped of `rarity`, exactly. The world secret is still absent from the payload by value (positive controls first), the helpers are not client-executable, and grants/executable-writers still zero. |
| **0033** | `the_yard_is_called_the_shipyard` | **THE YARD IS CALLED THE SHIPYARD.** The owner: *"what is yard?"* — jargon a player must ask about is a copy defect. Supersedes `cmd.do_repair` (0007, both refusals) and `cmd.verb_schema` (live = 0021's text with 0030's edits — the REPAIR card's help), sliced 0030-style out of `pg_get_functiondef` with each hunk asserted to occur exactly once. Deliberately left alone: the `E_NO_YARD` CODE and the `has_yard`/`yard_tier` columns (contract identifiers, not copy), the two prosrc comments (`cmd.advance`, `public.tick_arrivals` — pinned survivors), the server-only `repair_sim_hours_per_pct` description, and "yard" the spar. | Pre-slice positive control: the sweep sees the word in exactly the four named carriers before anything moves. Byte parity by construction, verified: substituting the old words back reproduces each pre-image to the character. Post-slice sweep: the standalone word survives in exactly the two named source comments. The served REPAIR card reads the new sentence with no standalone "yard" anywhere in the grammar. Both refusals provoked off the live verb on a rolled-back probe house, each speaking of the shipyard. ACLs byte-identical to their pre-images; grants, executable writers, entry-point gaps and caller-evaluated functions all zero. |
| **0034** | `a_standing_order_keeps_her_provisioned` | **THE STANDING ORDER.** The owner: "build set ratio but let me set it personally - adjust. Save it as preset. Give something like 6 presets" · "the ratio must account for number of crews as well" · "crew now, but show it clearly." `public.provision_presets` — a HOUSE-owned book of named orders ("keep her at N days of stores") capped by the `provision_preset_max` knob (6) through a trigger ON the table; a fleet points at one by REFERENCE (`fleets.provision_preset_id`, FK `on delete set null`), so editing a preset changes every fleet under it and a strike detaches them structurally. `cmd.run_standing_provision` (server-only) fires from `voyage.settle`'s arrival arm — the one place a fleet makes port, SLICED not retyped — after she docks and before her queued orders run, judged against `voyage.endurance_days` (the one range figure) and buying through `cmd.do_provision` in DAYS mode (the one cost/fit rule), sized at fire time from the crew aboard. The PROVISIONED event gains `standing`/`preset`/`days_target` when the order placed it (second slice); a short purse or a full hold writes `PROVISION_REFUSED` with the code and sentence. One read (`world.provision_presets`) and three verbs (save / delete / apply, apply-null = clear) join `client_rpc_entry_points`. | Both slices are their pre-images plus only the 0034 lines, by reverse-substitution to the character, ACLs unmoved. A probe house (rolled back, weather zeroed — 0031 rotates the secret, so hazards are a lottery a probe must not play): writes, re-days and renames one order; the SEVENTH is refused E_PRESET_CAP, a taken name and zero days refused, a foreign preset unappliable; a real arrival tops her to the target with the charge on the LEDGER against its own standing event; free hold falls by exactly the stores taken aboard; more crew means a bigger top-up from the same preset (5.04 t at 8 crew → 14.70 t at 20, range landing ≥ 30 d both times); a drained purse leaves a written PROVISION_REFUSED and buys nothing; the strike detaches 1 fleet; 0 client write grants, 0 unsanctioned writers, the 4 entry points authenticated-only, the executor server-only. Proof 07 re-proves it through the CLIENT door: RLS shows a house exactly its own book, twins on one voyage differ in free hold by exactly their gap in stores, and ONE edit moves BOTH fleets with no re-apply. |
| **0035–0037** | *(rows pending)* | The voyage-event catalogue, the sea places, and cmd.divert — each file's own header carries the full account; these rows were never added when the files landed. | See each file's self-assert receipt. |
| **0041** | `ten_islands_and_a_richer_catalogue` | **THE WORLD GROWS THROUGH A DOOR, NOT AN EDIT.** GENERATED by `scripts/build-world-growth.mjs`: the delta from the world the applied chain holds (measured by applying the real chain to a scratch PostgreSQL, never remembered) to the world `data/*.json` describes. Repairs the 2026-08-24 defect — 0003 had been regenerated in place AFTER production applied it, so production kept 70 goods/214 harbours while fresh rebuilds got 243, and the ten islands existed in no database; 0003 is reverted to the exact production bytes (git blob `eee9091`, byte-proven) and this file carries the growth: +10 island harbours (ANG CAG CHS HAD MLE MAT PLO TEN TID ZAK, codes PINNED in `scripts/lib/world-derive.mjs` after the derived assignment was caught stealing CHI/POR/PRT/TRI from four deployed harbours), +173 goods (~10 renamed), the roster rebalance (+606/−130 offers, dev_* re-derived), the regenerated leg graph (+94/−47/~621 re-measured), `port_goods` re-derived for EVERY (harbour, good) pair through 0005's one formula — missing pairs inserted, moved affinities re-set with stock := new target, untouched pairs left to the game — and the five affinity knobs reconciled to the tuned values (0005 was edited in place pre-rule; a deployed world may hold the old ones). Touches NO player row; crew_pool, stock-on-unmoved-pairs, drift, and 0036's sea places/spur legs deliberately left alone. | The END STATE EQUALS THE DATA by set equality with values, both directions, from generated literals: every harbour column-for-column, every good column-for-column, every offer pair (with a POSITIVE CONTROL — a planted bogus offer is seen by the certifying query, then removed), every harbour leg with distance/hazard/note. Deltas measured against a pre-image, exact on both legal baselines. The derivation RULES asserted over every live row (dev_* restate roster+size; the owner's 4–9 offers-by-size band; affinity re-derivable from `world.affinity_at` for all 54,432 pairs; 0005's stock arithmetic on every row). The graph still one piece from Lisboa over ALL ports; no leg beats its great circle; spur legs exactly as found; knobs at the tuned five; 0 client write grants / executable writers / read-wall gaps. |
| **0040** | `every_water_answers_its_sea` | **EVERY WATER ANSWERS ITS SEA.** GENERATED by `scripts/build-sea-raster.mjs`: `public.sea_cells` (720 inline rows × 1,440 bytes, one byte per 0.25° cell = `seas.raster_ordinal`, 0 = no navigable sea) with boundaries from Natural Earth marine polygons, unnamed water joined to its nearest sea BY WATER; `voyage.sea_at(lat, lon)` is the ONE membership lookup, strict (null on land), server-only. Seas gain `danger_level`/`note` (marked READ BY NO RULE TODAY). | 720 rows; every byte value names a sea and every sea has extent; every chain port resolves within 8 rings with the 17 port-vs-cell disagreements pinned BY NAME; six real waters answer by name and the Sahara answers null; the read wall unmoved. |
| **0045** | `the_world_runs_twenty_times_faster` | **time_compression 480 → 9600** (the owner: faster testing) — a voyage-day is 9 real seconds. Every SAILING voyage re-ETAd through `voyage.recompute_eta`, the one ETA authority, so stored arrivals and re-derived day boundaries agree. `game_day_seconds` deliberately untouched. | The knob is exactly ×20; a voyage-day derives to 9 s from the knob, never retyped; a real probe voyage re-ETAs LATER at 480 and EARLIER back at 9600 (both directions), rolled back leaving no row. |
| **0046** | `the_water_knows_the_way` | **THE NAVIGABLE SEA AS ONE RASTER** (regenerated from the helm worktree's 0038 against this chain). `public.sea_raster` (1440×720 × 2-bit passability mask: SEA + the unread POLAR margin), `public.sea_reaches` (every place's SAILED nm to every other place, one pathfinder flood per source over this very raster), and the two primitives every passage stands on: `voyage.path_nm` (THE measure) and `voyage.path_refusal` (THE water-legality sampler, half-cell steps, measured approach allowances). Cross-checked against 0040's sea_cells at generation over every reachable cell. | Raster shape + 8 named control cells through get_bit; the sailable controls also answer a sea through `voyage.sea_at` (0040 composed — drift between the two rasters refuses to apply); every place reaches every other, symmetric, never under the great circle; the Arctic is CLOSED (LIS→NAG ≥ 12,000 nm where the graph served 7,565 over the pole), no Suez, no Panama; the measure prices a real embedded course and the law refuses a straight Lisbon→Barcelona line as E_LAND, a mis-joined course as E_OFF_COURSE, a one-point course as E_BAD_PATH; grant posture unmoved. |
| **0047** | `the_sea_is_a_free_plane` | **ONE MOVER** (regenerated from the helm worktree's 0039 against this chain by `scripts/gen-0047.mjs`, slice-and-replace, hunks occur-exactly-once). The client PROPOSES a course; the server VERIFIES it against its own raster and MEASURES it itself. Any water point is a destination (`SAIL TO 33,-15`; she ANCHORS and can always leave; round-trip stores gate). DROPS `voyage.route`/`route_direct`/`path_from_nodes`/graph `reach_from`/`sail_refusal`/`depart`/`position`/node-turn `divert`; re-cuts `cmd.do_sail`/`issue`/`preview`/`world.trade_routes`/`cmd.divert` (turns WHERE SHE IS); slices `settle`/`advance`/`parse`/`verb_schema`/`snapshot`/`fleets`. `voyage.sea_near` COMPOSES 0040's sea_at (8-ring approach tolerance) — the helm cut's interim sea_at is never created. Pre-0047 voyages convert to legacy-marked segments, schedule byte-identical. The land guard `voyage.assert_paths_water` walks every stored course. | A real probe house through `cmd.issue` itself: a straight Iberia course refused E_LAND, an unplotted cape passage E_NO_COURSE, a mis-joined course E_OFF_COURSE; the real Lisbon→Cádiz course SAILS with server-measured total = its own segments = the reach quote (3%); the whole course + closed-form position served to the chart; a queued homeward SAIL departs itself; she pinpoints 33,-15, ANCHORS on the point, BUY refused E_NOT_DOCKED, sails home, thin stores refused for the ROUND TRIP; the helm answers mid-ocean (truncate at position, stale queue cancelled, new voyage bridged from where she stands); a leg-era voyage converts byte-identically; the land guard walks ≥4 voyages and BITES a planted straight-across-Iberia voyage; every trade recommendation priced at the reach table's nm; the graph authorities are GONE (asserted by name); wire + wall zero. |
| **0048** | `the_quay_reprices_the_honest_sea` | The affinity gradient retuned to honest distances (helm's tune-balance sweep, in the header): producer 0.93 / home 1.00 / span 0.76 / reach 9500 / curve 0.88, every `port_goods.affinity` recomputed through `world.affinity_for` — THE seeding function — stock CLAMPED down to fallen targets (never minted), drift untouched. | Knobs read back exactly; every stored affinity equals the one function over the whole table (≥1,000 rows); no stock above target; every produced good still cheaper at a source than at the far end of the world. |
| **0049** | `the_graph_is_history` | **`public.legs` IS DROPPED** (and `voyage.convert_leg_path` with it). The raster (0046) is the one authority for what water connects to what; 0047 left the table readerless, and a table nobody reads is drift waiting to be trusted. `data/sea-routes.json`, `scripts/build-sea-routes.mjs` deleted; the world guard stopped checking legs while KEEPING its planted-drift positive controls; `build-world-growth.mjs` stopped deriving legs and now refuses a grown port with no reach row. | Non-vacuous: ≥100 graph rows existed to drop; NO live function names `public.legs` (catalog scan, by name); no stored path is leg-shaped; the raster/reach/refusal authorities stand; every entry point resolves; the read wall reads zero. |
| **0050** | `a_refusal_is_two_numbers_and_a_verb` | **A REFUSAL IS DATA, NOT A PARAGRAPH.** The owner, of E_ENDURANCE's four-clause sentence: *"too long. make it very concise… Always show in graphics, concisely."* THE LAW: the client must NEVER parse a served sentence for numbers. So `voyage.sail_refusal` is SUPERSEDED to return `{code, sentence, figures}` as ONE value (0047's body, gate for gate, arithmetic unchanged to the character — only the SHAPE of the answer moves), `cmd.refuse(code, sentence, figures)` is the ONE raiser (figures cross in `PG_EXCEPTION_DETAIL`), `cmd.refusal_caught` is its inverse, `cmd.figures` is the ONE constructor and it RAISES on `have > need`, on a non-positive `need` and on a unit that is a sentence. `public.orders.error_figures` keeps a failed order's figures, because a failed order is history. The sentences lose the ARITHMETIC (E_ENDURANCE now contains no digit) and the FIX (`cmd.fixes()` has authored those since 0008) and keep the REASON. **What it paid for itself with:** "split a raised refusal into an envelope" had **SIX** hand-copied definitions — five found by reading, the sixth by this file's own catalogue guard — all six carrying the same latent bug (a non-`E_CODE:` message was still cut at `length('E_PARSE') + 2`, so *"division by zero"* reached the player as *"by zero"*). 14 live bodies sliced by occurs-exactly-once hunks. `voyage.path_refusal` deliberately NOT superseded: E_LAND/E_OFF_COURSE/E_NO_COURSE are not arithmetic. | `cmd.figures(2.9, 33, 'days')` is exactly `{"have":2.9,"need":33.0,"unit":"days"}` and BITES on all four malformed argument sets; `refuse`/`refusal_caught` are inverses **across a real RAISE**; a non-refusal message keeps its first eight characters (`'division by zero'` round-trips whole); the six hand-copied splits are **gone**, probed against COMMENT-STRIPPED code; a real house on thin stores is refused E_ENDURANCE through `cmd.issue` with served `have` = `voyage.endurance_days` itself, a sentence carrying **no digit**, **≤ 80 characters**, still naming *chandler*, and ≥ 1 fix; the failed order in the served queue carries the same figures; `preview` and `issue` agree digit for digit; the `world.trade_routes` seam measured as a DELTA (`ports_considered` strictly greater on full casks); a BUY over the hold refused E_HOLD_FULL in the hold's own unit; posture zero on all three authorities and `cmd.refuse` unreachable by either client role. |
| **0051** | `the_world_says_how_rare_rare_is` | **RARITY BECOMES A LADDER OFF THE WORLD'S OWN PRODUCER DENSITY.** 0032's thresholds were three ABSOLUTE producer counts (≤2/≤5/≤12) calibrated for 70 goods; at 243 goods over 1,310 specialty rows they tiered the catalogue **exotic 133 (54.7%) / rare 58 / uncommon 27 / common 25** — the top tier was the default, and the word meant nothing. 0032's own shape assert allowed any tier up to 60%, so the meaningless rule satisfied it; **that loose assert was part of the defect and is re-cut here to fail on it.** THE RULE: `public.rarity_scale()` is the world's mean producer count (μ = 5.391 today, read from `port_specialties` alone) and the cuts are `max(1, μ/4)` / `max(2, μ/2)` / `max(3, μ)` — a geometric ladder floored at the smallest three whole numbers of producing ports. Today: **exotic 47 / rare 86 / uncommon 58 / common 52**, 171 goods re-tiered. SUPERSEDES 0032: `rarity_from_producers(int)` is **DROPPED, not overloaded**, so no caller can pick between the old law and the new; the law stays IMMUTABLE because the table read was lifted OUT of it into an argument. `world.snapshot`/`world.market` are NOT re-cut — they already ask the one authority. | Scale-freedom: every good keeps its tier when the whole world is multiplied by **k = 2/3/7/17/50**, with **0032's absolute rule run through the same sweep as a positive control** (at k=17 it puts 100% of the catalogue in one tier). All four tiers reachable at every scale from μ=1 to μ=1000. The law pinned as arithmetic on both sides of every boundary at μ=8 and μ=20, and at the floors μ=1 and μ=3.9. Run BACKWARDS on 0032's own world (70 goods, 834 rows, μ=11.914) the rule derives ≤2/≤5/≤11 against the ≤2/≤5/≤12 chosen by hand. The re-cut shape assert refuses an apex over 25% and any tier over 40%. `rarity_from_producers(int)` no longer resolves. The catalogue is proven to have actually MOVED (171 goods), and snapshot + market payloads with `rarity` stripped are byte-identical to pre-images. Posture zero. |
| **0052** | `the_severn_is_water_and_the_pack_is_one_rule` | **GENERATED** (`scripts/build-sea-migration.mjs`). Supersedes the DATA of 0046 — `sea_raster`'s one row rewritten, `sea_reaches` replaced wholesale — and **re-cuts no function**, because a generator that re-emitted the bodies it remembered would silently roll 0050 back. **DEFECT 1: Bristol sailed overland.** The Severn is narrower than a 0.25° cell above Barry, so the whole Bristol Channel east of 4°W scan-filled as LAND and Bristol's nearest sailable water was **Lyme Bay, 64.55 nm away over Devon**. `snap_nm` is what `voyage.path_refusal` grants a course as its head allowance, so she carried a **~90 nm land-exempt corridor and the pathfinder used it**: BRS→AMS 324.7 nm whose first leg ran STRAIGHT from the quay across Somerset, Dorset and Hampshire; BRS→DUB 179.5 nm over the Welsh mountains. That is a live breach of the owner's law (`OWNER_REQUESTS` row 41, *"i don't want the fleet to ever touch land"*), not the cosmetic labelling defect D22 filed it as. A `severn` CHANNELS entry opens **5 cells (545,984 → 545,989)**: snap **0.00 nm**, her own sea at **ring 0**, BRS→AMS **613 nm round Land's End**. **DEFECT 2: the Antarctic closure was three statements** (`cells.fill(0)` at −60, and the cross-check's own −60); `ICE` gains `latBelow` and the pack becomes ONE row in it, justified from the dates. Also patches the 5 cells of 0040's membership raster the Severn opened, each joined to its nearest named sea BY WATER. | Raster shape 1440×720 × 2 bits = 259,200 bytes; **13** named control cells through `get_bit`, including the Bristol Channel off Barry and the Severn at Bristol OPEN, the Welsh hills CLOSED, and the pack asserted from BOTH sides (59°S open, 61°S ice). The 5 newly opened cells each answer **North Atlantic Ocean by name** through `voyage.sea_at`. Every place reaches every other, 0 asymmetric readings, 0 pairs under the great circle. The geography gates hold: LIS→NAG ≥ 12,000 nm, no Suez, no Panama. Bristol's snap must not exceed 20 nm (*it was 64.55*) and BRS→AMS must not fall under 500 nm (*it served 324.7*) — both non-vacuous by construction. The real overland BRS→AMS course is REFUSED as land. **THE WATER DID NOT MOVE where it was not meant to:** 55,938 pair readings identical, 468 moved and every one of them Bristol's; 0 cells differ from the old Antarctic rule; the Horn, the Drake Passage, the Roaring Forties and the Cape route identical to 0.1 nm. |
| **0053** | `the_quay_prices_its_neighbours_once` | **THE MARKET SCREEN STOPS ASKING THE SAME QUESTION 9,720 TIMES.** `world.market()` drew one screen by calling `world.pct_of_neighbours` 243 times, which called `world.mid_price` 9,720 times, each of which re-read **four `public.wc` knobs** — **38,880 plpgsql calls for four constants that cannot change during the statement, and 62% of the mid's cost**. Measured at Bordeaux: 1,442 ms, 336,872 buffers, **91% of them inside the %NBR CTE**. The §G.1 mid arithmetic moves into `world.mid_from_terms` — SQL, IMMUTABLE, no SECURITY DEFINER and no SET clause, **which is exactly what PostgreSQL needs to INLINE it** — and %NBR gains a set-valued body, `world.pct_of_neighbours_at`, that derives the neighbourhood and the five knobs ONCE per quay. The scalar keeps its name and COMPOSES the set through a nullable narrowing argument; nothing is copied. 0019's CTE fence is NOT the problem and is kept. Bordeaux **1,442 → 241 ms**, buffers **331,470 → 40,530**. | **Value identity four ways, against pre-images captured in the same transaction**: all **54,432** mids in the world; a 7,290-row hypothetical-stock sweep (0, 1, half, triple, 1e9); **268** answers through the scalar door; and the FULL `world.market` payload **byte for byte at six ports**, including the crowdedest neighbourhood and an empty one. The guard on the work is the **buffer RATIO and the deployed shape, never a millisecond**: `explain (analyze, buffers)` on the same call before and after in one transaction, the new body touching at most a THIRD of the old buffers (measured 8.2×; the guard demands 3× because the break-test harness reads 5.8×; the old shape scores about 1×). Shape off the catalogue: `world.market` names the set authority exactly once and the scalar not at all; the scalar contains no haversine and no mid; `world.mid_price` contains no `power(`. **The inline precondition is asserted** — SQL, IMMUTABLE, not definer, no proconfig — because otherwise 9,720 real function calls silently return. Posture zero. |
| **0055** | `what_these_waters_breed` | **THE SEA DECIDES WHAT BEFALLS A FLEET IN IT — AND IT LANDED DARK.** `public.seas` carries four spatial facts; only `hazard_base`/`piracy_index` were read, and those took **exactly three distinct value-pairs across all 51 seas — 71 per cent of the world's water was mechanically identical**, so the Strait of Malacca (the game's only danger 5) drew from the same bag as the Baltic. `seas.danger_level` and `seas.note`, authored per sea by 0040 for exactly this, were READ BY NO RULE. Each kind row gains three response numbers and the mix is DERIVED — `mix_base × (1 + mix_danger·D + mix_raiders·D·piracy)` — so there is no per-(kind, sea) weight table free to drift from the danger scale it would restate (that table would have been 51 × 8 = **408 hand-authored numbers**). **`mix_raiders` is an INTERACTION and that is the point:** a plain piracy term puts corsairs in the Arctic; raiders need lawless water AND hard water at once. **Ten distinct mixes where there were three.** Five new kinds against 0035's catalogue — **FAIR_WIND (the first event kind that is not a loss)**, CONSORT, DERELICT, FOUL_WATER, SHOAL_WATER — all `is_rolled = false`, 0035's own retirement switch. Supersedes ONE function, `world.fleets()`, by ONE sliced hunk adding `'waters'`. **Lighting it is one migration and four statements, named in the header**; measured cost, Barbary raid-days **43.0% → 20.4%** of event-days. | **It is DARK, and asserted so**: `voyage.hazard_roll`, `voyage.settle` and `voyage.report_line` byte-identical after this file; the rolled set still exactly {STORM .40, CALM .35, PIRATES .25}; the live draw still answers 0006's CASE on all **50,000** (r, piracy) pairs; `voyage.encounter_at` has **0** callers and `voyage.sea_mix` exactly one (encounter_at itself), probed on comment-stripped `prosrc` with a positive control. **No voyage yields one ducat differently because this file ran.** The mix is TOTAL (every sea returns all 8 kinds, shares > 0 summing to 1) and it VARIES (Malacca beats the Baltic by > 15 points, the Arctic stays under the Mediterranean). All 5 new kinds read back out of `voyage.report_line` on real payloads with no code edit. `encounter_at` is pure in (voyage, day, secret): identical answers after the voyage is moved 600 hours through the clock, drawing exactly the three rng streams. The `world.fleets` slice is proven to be **exactly that one insertion**, byte for byte, ACL re-issued and unmoved. Proof 01 gains a sixth marker, `OFFLINE_EQUIV_ENCOUNTER`. |
| **0056** | `the_noise_stops_drowning_the_geography` | **`drift_sigma` 0.04 → 0.02 — one row of one table, and it SUPERSEDES the value 0001 seeded at 0001:159 and nothing else.** Why: proof 05 reported a first voyage paying 12–18% for a week, and pinning its market (the 0054 slice) showed that figure was **an artefact of the harness — a measurement of how many drift ticks it happened to run**. A freshly applied chain has stepped the drift ONCE on the 14,980 rows 0003 seeded and NEVER on the 39,468 that 0041 added, so **72% of its prices sit at exactly drift 0**. A world whose clock has run — every deployed world — sits at the OU stationary law `0.04/√(1−0.81) = 0.0918` and pays **37.4%**. **And the deciding number is not that one:** `BALANCE_DISTANCE_PAYS` had fallen to **18.87% long vs 16.19% short — 1.17×** where one drift step showed the world's own geography is worth **3.31×**. The noise had all but erased the reason to leave home waters. Rejected, with figures: the affinity knobs (they author the gradient and cannot touch this — with drift out, the authored economy pays 7.0% against the 7.5% 0005:125 tuned it to), widening the spread, `drift_theta` (memory, not size), `drift_clamp` (a guard rail, not an amplitude), and moving the band instead. Touches no function, no other knob, and no stored price — every price is derived on read. | **The self-assert does not read the knob back, it watches it work.** The write happens INSIDE the assert block (0031's shape) so the knob table can be photographed before and after: exactly ONE row differs and it is `drift_sigma` — asserted by value comparison in both directions, because the first draft asserted it from `updated_at` and `breaktest-0056.mjs` walked straight through that. Then the market is flattened and the chain's OWN `tick_market_drift` is run: sd must land within 0.9–1.1× of 0.02 on **≥ 14,000** rows moved, and **the SAME probe with 0.04 restored must NOT land in that window** — the positive control that makes the first number mean anything. Both probes unwind to a savepoint; only the number escapes. The settled amplitude must halve exactly (0.0918 → 0.0459, ratio 2.0) and the clamp must still stand at ≥ 3 sd of the new law. `BALANCE_MEDIAN_IN_BAND` moves to 13.0–20.0 in the SAME commit, so the knob and the band that judges it are never in two states at once. |
| **0057** | `the_record_keeps_only_what_the_disk_can_afford` | **PRODUCTION HIT ITS DISK QUOTA AND WENT READ-ONLY.** `public.price_history` was 1,410 MB of a 1.43 GB database (98%), because `tick_price_snapshot`'s prune window, `world_config.price_history_slots`, was pinned at 288 in 0013 for a 14,980-pair world and never resized when 0041 grew the world to 54,432 pairs — its designed ceiling was 15.7M rows / 2.94 GB. The window stops being a typed constant: `public.price_history_window_for(pairs)` derives `greatest(48, floor(600 MiB / (pairs × 201 measured bytes/row)))` — 48 is `src/lib/rpc/index.ts:289`'s one client caller, the only floor the game needs; `public.price_history_window()` reads `port_goods` and hands the count to the law, the same split 0051 used for `rarity_scale`/`rarity_from_producers`. `public.tick_price_snapshot` (0013:97) and `world.price_history` (0013:139) are SLICED with `pg_temp.recut`, one line each, to read the new authority instead of `wc_int('price_history_slots')`; the knob is DELETED once both readers are gone. The migration PRUNES FOR REAL, in its own transaction — `VACUUM` cannot run inside one, so the freed pages are reused, not returned; that needs a manual `VACUUM FULL` afterward. | At today's 54,432 pairs the window falls 288 → 57 and the table's ceiling falls 2.94 GB → 594.7 MB (a 58% cut from the 1,410 MB that caused the outage). Non-vacuity: a real precondition is SEEDED (three slots, one real port — a fresh chain boots price_history EMPTY). SCALE-FREE: swept at k = 1/2/3/7/17/50 of the real pair count plus the sparse end (1/10/100 pairs), the window never falls under 48 and never costs more than the budget above that floor; a POSITIVE CONTROL proves the sweep is not blind — the retired constant (288) at today's real pairs costs 3,150,959,616 bytes against the 629,145,600 budget. Both re-cut bodies are read back off `pg_get_functiondef`: each names `public.price_history_window()` exactly once and neither, nor any `pg_proc` body in the whole catalogue, still names `price_history_slots`. VALUE PARITY: `world.price_history(port, 48)` served byte-identical payloads across the slice, over identical data. THE PRUNE ACTUALLY PRUNED: a row seeded 100 slots old is gone; the recent seed survived. THE TABLE IS BOUNDED: after the real prune, `max(slot) − min(slot)` sits inside the current window. Posture unmoved: 0 client write grants, 0 client-executable writers, the two new law functions unreachable by any client role, `world_config` no longer carries the retired knob. `scripts/db/breaktest-0057.mjs` breaks all fifteen guards on purpose and watches every one go red. |
| **0058** | `a_city_offers_what_its_size_earns` | **THE ROSTER COUNT BECOMES A LAW, NOT A HAND-TYPED BAND.** The owner, `docs/OWNER_REQUESTS.md` row 48, repeating row 38 with harder numbers: *"capital cities - 10 items, mid sized cities - 4~8, small cities 4, randomly distributed."* 0041's generator (`scripts/build-world-growth.mjs`) had shipped a single 4–9 band for every tier; this file deletes it. `public.roster_target_count(tier, authored, draw)` is the ONE place the three numbers exist: capital (`size_tier` 5) always 10, small (`size_tier` 2) always 4, mid (`size_tier` 3) keeps an already-legal authored count (4–8), trims an over-band one to 8, or seeds one across 4–8 when there is none to keep. **`size_tier` 5/3/2 = capital/mid/small, cited from `scripts/lib/world-derive.mjs:16`, not guessed.** The seeded draw, `public.roster_rng(key)`, composes `voyage.rng_raw` (0006) rather than re-deriving its md5 arithmetic — and deliberately is **NOT** keyed on `world_config.world_secret`: MEASURED that 0031 rotates it on every fresh apply (two consecutive `db:apply` runs produced different secrets), which would make the roster differ across every fresh boot and fail `world-guard`'s byte-for-byte requirement against `data/*.json`. Every ADDED/DROPPED offer ripples `public.port_goods.affinity`/`stock_target`/`stock`/`production_rate` (0048's own recompute pattern, composed) and `ports.dev_commerce`/`dev_industry` (`scripts/lib/world-derive.mjs`'s own formula, restated because its input changed) — closing a gap 0048 itself had left in `production_rate`. `data/ports.json`'s `goods` arrays were updated for exactly the 113 ports that changed (35 capitals gained filler, 78 small harbours lost one), because `world-guard.mjs` has no notion of "authored plus derived filler" and a static file that disagreed with a deterministic function would fail on every apply. | Every one of 224 harbours now satisfies its tier exactly (10 / 4–8 / 4), 0 below the owner's floor, 0 `SEA_PLACE` rows touched (14 zero-offer rows in the original brief were re-verified to be sea anchorages, not cities — `kind = 'SEA_PLACE'`, no market, `E_NO_CHANDLER` since 0036 — not the 14 broken harbours the brief assumed). 78 offers dropped, 56 filled (1,310 → 1,288 total). The retired band (capital ≤9, small ≤5) now DISAGREES with 79 ports — a positive control, not a coincidence. The mid-tier draw spreads across ≥ 3 distinct values over 200 synthetic keys (real mid harbours never exercise the draw-branch today). Determinism: `roster_rng`/`roster_target_count` are IMMUTABLE by catalog, and every harbour is a fixed point of its own law (re-running the assignment on the world this file produced moves nothing) — confirmed empirically too, two independent fresh `db:apply` runs produced byte-identical roster/rarity numbers despite different `world_secret`s. `port_goods` and `dev_commerce`/`dev_industry` restate the new roster with 0 disagreements. Rarity (0051) still shows all four tiers inhabited, apex under 25%, no tier over 40% — **mu 5.391 → 5.367, exotic 47→44, rare 86→75, uncommon 58→73, common 52→51** — the apex and the biggest tier both moved further inside the guardrails, not closer to the edge. Posture zero. `scripts/db/breaktest-0058.mjs` watches every guard go red. |
| **0059** | `the_sea_decides_what_it_breeds` | **THE MIX 0055 LANDED DARK IS LIT, AND IT IS EXACTLY THE FOUR STATEMENTS 0055's OWN HEADER NAMED** (`20260818000055_what_these_waters_breed.sql:132-143`, quoted verbatim in 0059's header). (1) `voyage.hazard_roll`'s body becomes `voyage.encounter_at`'s — not retyped but SLICED, the deployed definition with only the name in its header changed — and `voyage.encounter_at` is DROPPED in the same statement, because 0055 wrote it to BE this body and a copy kept "for reference" is a second author. (2) The flat draw is DELETED, not left beside the mix: the closure trigger `voyage_event_kinds_weights_close`, its function `public.tg_voyage_event_kind_weights`, and the columns `roll_weight`, `cedes_to` and `cede_fraction` — the cede columns too, on 0055:427-430's own ruling that "the mix says it directly, so a second mechanism for the same sentence would be a second authority" — then `is_rolled = true where in_sea_mix`, so `is_rolled` now means only "drawn". (3) `voyage.settle` gains five arms by a three-hunk slice: FAIR_WIND gives back delay hours, FOUL_WATER starts water over the side, SHOAL_WATER takes durability, DERELICT and CONSORT touch no ship; `if v_delay > 0` becomes `<> 0` because a fair wind moves the arrival EARLIER; and the chain ends in an `E_KIND_ARM` raise so a drawn kind with no arm can never be written down as a quiet watch (§7C). (4) `voyage.waters_ahead` serves the mix as a fifth fact per row. **FAIR_WIND is bounded twice, and both bounds are load-bearing**: never more than `voyage.delay_before_day` (0006:475), because `voyages.delay_hours` is `check (delay_hours >= 0)` (0006:67) and an unbounded gain aborts the settlement; and never a full voyage-day (6–22.8 h), because a gain of 24 h or more moves day d+1's boundary to or before day d's and the schedule INVERTS — the first draft gained up to 48 h and its own probe caught it. | MEASURED by walking all 10,000 points of [0,1) twice, once through the bands captured one statement before they were deleted and once through the deployed `voyage.sea_mix`: **PIRATES 43.0 → 20.4 per cent of Mediterranean event-days** (the Barbary run), Baltic 33.0 → 7.0, Malacca 43.0 → 25.8, Caribbean 43.0 → 23.4, Arctic 29.8 → 9.3; 7,540 of 10,000 points of the Mediterranean draw now land somewhere other than the flat bands put them. All 51 seas answer with all 8 kinds, every share positive, closing on exactly 1. **THE FREQUENCY DID NOT MOVE**: over 34 voyage-days of a real 3,536 nm two-ocean passage the lit draw matched DESIGN B.6's clamp and the three rng streams **recomputed independently of the deployed body** on every day. Every one of six arms fired on a REAL settled day with the mix pinned to it, each moving exactly what its own payload reports and nothing else, and each reading back as a sentence out of `voyage.report_line` with no code edit; a kind with NO arm was drawn on a real day and REFUSED with `E_KIND_ARM`. `voyage.settle` is its pre-image with exactly the three declared hunks made (4,785 inserted characters, everything outside them byte-identical), and `sea_mix` / `report_line` / `recompute_eta` / `delay_before_day` are byte-identical. Posture zero. `scripts/db/proofs/01_offline_equivalence.sql`'s `OFFLINE_EQUIV_ENCOUNTER` marker follows the body to `voyage.hazard_roll` and now covers the LIVE draw. `scripts/db/breaktest-0059.mjs` breaks all fifteen guards on purpose and watches every one go red. |
| **0061** | `a_city_sells_only_what_its_roster_names` | **A CITY SELLS ONLY WHAT ITS ROSTER NAMES.** 0058 gave every harbour a 4-10 good roster and made it a law, but the roster only set AFFINITY: `public.port_goods` still carried one row per (harbour, good) — **54,432 = 224 x 243, measured** — so `world.market` listed all 243 goods at every city and `cmd.do_buy` sold any of them anywhere. The owner said it twice (`docs/OWNER_REQUESTS.md` row 48), which is what a repeated instruction always means. `public.port_offers(port, good)` becomes THE authority for *is this good on this city's quay?* — it READS `port_specialties` and derives nothing, so it survives the restoration of `data/ports.json`'s authored lists. `cmd.do_buy` gains ONE gate raising the **E_UNAVAILABLE it already raises for the culture mask** (0007:435-443), `world.market` serves the quay through `public.quay_shows` (the roster PLUS whatever a fleet of the reader's lies here CARRYING, so a hold is never stranded) and each row carries `offered`, `world.trade_routes`'s ORIGIN CTE asks the same authority — a defect **found by `scripts/db/proofs/04`, not by reasoning**: the scan joins `port_goods` at both ends and every pair still has a row, so it went on naming cargoes `cmd.do_buy` would refuse (*"the Lisboa market served 10 of 243 goods, civet is on it 0 time(s)"*); its DESTINATION is deliberately left open, because selling is not gated. And `public.tick_price_snapshot` records only offered pairs. **`cmd.do_sell` and `public.port_goods` are deliberately NOT touched**: the market row IS the price, and a city that refused to BUY outside its roster would leave a hold with ~5 buyers in a world of 224 harbours. | `port_offers` agrees with the roster on **all 54,432 pairs** (1,288 offered / 53,144 not) and gives no sea place a quay; every harbour offers **4..10** goods — the owner's own band, deliberately NOT the per-tier counts, because the roster's CONTENT is being restored separately; `world.market` at one harbour per tier serves that harbour's roster EXACTLY (ACC 4, ACA 6, ALE 10) and exactly one row wider when a hold carries something the city does not trade, with every `offered` flag agreeing with the authority. On a real house at Lisboa: a queued BUY of `abaca` — whose market row still exists — is **refused E_UNAVAILABLE and the order recorded `failed`**, `black-pepper` still fills 10 tuns for 1,295 d. with the purse moving by exactly that, and **SELL of that same `abaca` still pays 2,523 d. for 20 tuns**, so no hold is stranded. `port_goods` is unchanged at 54,432 rows and an un-offered pair still prices. `price_history` **54,918 -> 2,588** rows and the snapshot now writes **1,288** (was 54,432): at 0057's untouched 57-slot window and 201 bytes/row the ceiling falls **~594.7 MiB -> ~14.1 MiB**. Posture zero. `scripts/db/breaktest-0061.mjs` breaks every guard on purpose and watches each one go red. Two proof pins moved deliberately off a membership assumption and onto a derived one: proof 4's *"the prices screen still has to price EVERYTHING"* became the port's own roster count with the owner's 4..10 band beside it, and proof 5's EXHAUSTIVE control now scans the goods a captain could actually LOAD (it had been beating the shortlist with *cloves at Alexandria*, a trade nobody can make). |

---

## 2026-08-22 — 0017 found a grant hole that had been open since 0001

0017's posture assert (README §3: *"assert `REVOKE`d state explicitly; do not assume the default"*)
failed on its first apply, on a claim that should have been free:

```
0017 self-assert FAIL: authenticated may execute public.fleet_free_hold
```

Measured on PostgreSQL 18.3, chain applied through 0016 with `scripts/db/supabase-preamble.sql` in
place: **`pg_default_acl` holds three rows and every one of them is owned by `supabase_admin`.
There is no row for the role that applies this chain.** So 0001 §5a's

```sql
alter default privileges in schema public, world, cmd, voyage
  revoke execute on functions from public, anon, authenticated;
```

recorded nothing, and every function created since which did not carry its own explicit `revoke`
was left at PostgreSQL's built-in function default — **EXECUTE for PUBLIC**.

The tables half of the same statement is harmless by luck: the built-in default for a *table*
grants nobody anything, which is why `client_write_grants()` has honestly read zero all along. It
reads table grants and is blind to this by design. Functions were the exposed half, and **22
`SECURITY DEFINER` functions that WRITE were executable by `anon`** — among them
`public.fleet_load`, `public.fleet_unload`, `cmd.do_buy`, `cmd.do_sail`, `cmd.issue`,
`cmd.execute_order`, `voyage.depart` and `cmd.advance`. None of them checks who is asking, because
0007 and 0008 rely on `cmd.issue` doing that upstream — which is exactly the assumption a direct
call breaks.

**0017 closed the seven it re-cut** (`public.ship_hold_capacity`, `public.fleet_free_hold`,
`public.fleet_load`, `public.fleet_buy_capacity`, `cmd.do_buy`, `cmd.do_sell`, `cmd.do_provision`)
and re-granted the two the client really calls (`world.quote`, `world.fleets`) to `authenticated`.
It did **not** attempt the other 17: a blanket revoke written blind is how a deploy takes the game
offline, and re-granting the chain is its own concept. They are printed as a permanent `NOTICE`,
by name, on every apply — 0001 (d2)'s shape, never swallowed and never fatal, because an assert
that can never pass is pressure to delete the check.

**Those seventeen were closed by 0018**, which is the migration that paragraph asked for:
`cmd.advance`, `cmd.cancel_at`, `cmd.clear`, `cmd.do_hire`, `cmd.do_repair`, `cmd.do_sail`,
`cmd.execute_order`, `cmd.issue`, `cmd.preview`, `public.fleet_unload`,
`public.tg_reconcile_from_ledger`, `public.tg_reconcile_from_player`,
`voyage.assert_sailing_invariant`, `voyage.depart`, `voyage.recompute_eta`, `voyage.settle`,
`world.ledger`. `proofs/03_grant_lockdown.sql` grew the ninth marker as well:
`GRANT_LOCKDOWN_ANON_CANNOT_EXECUTE`.

## 2026-08-22 — 0018 closed it, and found that the culprit was two words

### How bad it actually was, measured

The chain applied through 0017, then `set local role anon` and the call made:

| as `anon` | answer |
|---|---|
| `select public.fleet_unload(null, null, null)` | **ANSWERED. No error.** It writes cargo. |
| `select cmd.do_sail(null, null)` | 42501 permission denied for **schema** cmd |
| `select cmd.issue(null, null, null)` | 42501 permission denied for **schema** cmd |
| `select world.ledger(null, null)` | 42501 permission denied for **schema** world |
| `select voyage.depart(null, null, null)` | 42501 permission denied for **schema** voyage |

`anon` holds USAGE on `public` and on nothing else (0001:222-225), so in three of the four schemas a
second door happened to be shut — **not** by the lock that was supposed to be doing the job, and not
shut at all on `public`, which is the schema PostgREST exposes by default and where a
`SECURITY DEFINER` function that writes ship cargo was genuinely reachable by an anonymous caller.
That is why proof 03's new marker **grants `anon` USAGE on the three closed schemas first**, inside
the transaction it throws away: otherwise the sweep collects its 42501s from the schema door and
would stay green with EXECUTE handed back to `anon` on every function in three schemas.

### The culprit was `IN SCHEMA`

Four variants, each on a fresh PostgreSQL 18.3 (PGlite 0.5.5) holding the client roles and the four
schemas, each followed by `create function` in `public`, `world` and `voyage`:

| statement, issued as the migration role | `pg_default_acl` | the function it then bore |
|---|---|---|
| 0001:240 verbatim — `... IN SCHEMA public, world, cmd, voyage REVOKE EXECUTE ON FUNCTIONS FROM public, anon, authenticated` | **0 rows** | `proacl` null; anon, authenticated **and** service_role may all execute |
| the same, preceded by a per-schema `GRANT EXECUTE ON FUNCTIONS TO postgres` so a row certainly exists | 4 rows, `{postgres=X/postgres}` | `proacl` null; **still** all three may execute |
| the same statement **without `IN SCHEMA`** | 1 row, `defaclnamespace = 0`, `{postgres=X/postgres}` | `proacl {postgres=X/postgres}`; **all three denied** |
| the same, preceded by a global grant to the owner | identical | identical |

So the defect was never the REVOKE or the role list. A **schema-scoped** default ACL is added to what
`acldefault()` already gives — which for a function is EXECUTE for PUBLIC — while the **schema-less**
entry replaces it. 0001 wrote the one form that cannot express "PUBLIC gets nothing".

**The second row of that table is the trap, and it is why 0018 does not stop at reading
`pg_default_acl` back.** Four rows appear, they read `{postgres=X/postgres}`, a catalogue assert
passes — and a function created a line later is still executable by `anon`. A recorded row is not
the property. 0018's governing assert is therefore behavioural: it creates a probe function *after*
the fix and requires `has_function_privilege` to say no for both client roles. The catalogue read
sits beside it as corroboration, never as the proof. (The first draft of 0018 did exactly the wrong
thing here and its own assert caught it on the first apply.)

**Blast radius, stated rather than discovered later:** without `IN SCHEMA` this governs every schema,
not only these four. It still binds **only** objects created by the role that applies this chain — a
`pg_default_acl` row applies to its own grantor's objects and nobody else's (§5b, measured there) —
so nothing `supabase_admin` creates is touched. From 0019 on, a function is born executable by
nobody but the owner: it needs an explicit `GRANT EXECUTE ... TO authenticated` **and** a row in
`client_rpc_entry_points()` to reach the client, and an explicit `GRANT ... TO service_role` if a
tick calls it. `service_role` loses the PUBLIC ride it has had until now; the four grants already
written (0007:1059, 0010:160-162) are untouched.

### What 0018 deliberately did not do

Eight functions in these schemas are **not** `SECURITY DEFINER`, and seven of them stay executable by
PUBLIC: `cmd.fold`, `cmd.parse_number`, `voyage.gc_distance_nm`, `voyage.report_line`,
`voyage.rng_raw`, `world.affinity_at` are IMMUTABLE pure arithmetic or string folding that run as the
**caller** (so RLS applies and they reach no row the caller could not), and `public.forbid_mutation`
is a trigger body. `voyage.rng_raw` takes the world secret as a *parameter* precisely so calling it
discloses nothing. They are NOTICEd by name on every apply rather than left invisible.

It also **revoked `world.quote` from `authenticated`**, which 0017:418-419 had granted while 0017's
own header (`:90-91`) recorded that the client never calls it. The header was right;
`src/lib/rpc/catalog.ts` does not name it, and every money-moving path reaches it as the definer.

---

## The five proofs

`scripts/db/proofs/` — run by `npm run db:proof`, each inside a transaction that is rolled back.
Green means **every `-- @pass` marker the file declares actually appeared**; a file that declares
none fails as vacuous.

| Proof | Declares | Establishes |
|---|---|---|
| `01_offline_equivalence.sql` | 5 markers | DESIGN Appendix 2 §1. The **same** voyage — pre-screened to contain a real hazard — is settled day by day, the result captured, the settlement rolled away, and then settled **once, nine hours late**. `(day_index, kind, payload, resolved_at)` match to the character; so do the purse and the ETA. |
| `02_ledger_reconciliation.sql` | 6 markers | DESIGN Appendix 2 §2. 500 randomised orders across 3 houses with time jumping forward underneath them. Requires both successes **and** refusals and money moving both ways, then `purse = Σ ledger.ducats_delta` exactly — and finally falsifies a purse by **one ducat** to prove the check bites. |
| `03_grant_lockdown.sql` | 9 markers | DESIGN Appendix 2 §3. Not a catalogue query: it **becomes** `anon` and then `authenticated` and tries to INSERT into all 24 tables, requiring SQLSTATE **42501** specifically — a writable table would have failed 23502 instead. Also: `world_config` unreadable, RLS on every table, `service_role` still able to write, and — at end of chain, where it matters most — **every object in all four schemas owned by the role that applied the chain**, which is what makes Supabase's un-revokable default privileges harmless (see the 2026-08-18 note). Since 0018 it does the same for **EXECUTE**: it becomes `anon` — with USAGE on the three closed schemas deliberately granted first, so the function grant is what is under test — and calls **every** `SECURITY DEFINER` writer the catalogue holds with all-null arguments, requiring 42501 on each; then calls `cmd.issue` as `authenticated` and requires that it is **not** refused, because a lockdown that takes the game's only mutating entry point is an outage, not a lockdown. |
| `04_first_session.sql` | 9 markers | §K.1's ten minutes, replayed as real strings through `cmd.issue()`. Two honest divergences from the script are asserted rather than papered over: an oversized BUY does not fit a 60-tun hold carrying stores, and a laden Barca is slower than the 4.7 minutes quoted for an empty one. The house comes home **richer on an 8,000 d. stake**, by a printed number. **Since 0019 the session plays the trade the QUAY names**, both legs, and picks the destination that also offers a cargo home — it used to pick the widest affinity gap, which is not a profit, and the round trip therefore lost money on roughly one run in twenty. |
| `05_first_voyage_balance.sql` | 4 markers | **Balance, measured rather than argued.** The first version of this world paid 32% of the stake for one twenty-five-minute round trip. Across a sample of starting ports it now requires: every port offers a first voyage that pays; the **median return sits inside 4.0–16.0 per cent**; and **the long legs out-earn the short ones**, or there is no reason to leave home waters. The knobs behind it are swept by `scripts/db/tune-balance.mjs` — if this goes red, read that table rather than nudging a constant until the red goes away. **Since 0019 it no longer runs its own search**: the balance is measured on `world.trade_routes`, which is what the player is actually offered, and a fourth marker scans one port EXHAUSTIVELY and requires the read's shortlist to find a trade at least as good. The median moved 9.7 → ~11 per cent when it stopped halving from the free hold and started pricing at `fleet_buy_capacity` — the economy did not move, the measurement got honest. |

## Two defects these proofs found

Recorded because a proof that has never caught anything is not yet known to work.

1. **`settle()` could dock a fleet with checkpoints unresolved.** A voyage's last day rarely divides
   evenly into 24 sim-hours, so the final boundary could fall after the ETA — and the *number* of
   checkpoints then depended on whether settle ran day by day or once at the end (10 against 12).
   Proof 1 caught it. Fixed by clamping `voyage.day_ends_at()` at the ETA (0006).
2. **`ALL`/`HALF` resolved at parse time** would have frozen a stale quantity into a queued order —
   `SELL cloves ALL`, typed in Lisboa, meant "the number aboard right now" rather than "whatever is
   aboard at Amsterdam". Fixed by `cmd.resolve_qty()` reading the hold at execution time (0007).

## 2026-08-18 — 0001 amended twice: an over-broad assert, and the honest form of it

CI's disposable-Supabase job failed applying 0001, on 0001's own assert:

```
ERROR: 0001 self-assert FAIL: 16 default ACL entr(ies) would grant a client role a
       write/execute on future objects (SQLSTATE P0001)
```

Supabase ships `GRANT ALL ON TABLES/SEQUENCES/FUNCTIONS TO anon, authenticated, service_role` in
`public`, issued by its own bootstrap role `supabase_admin` — 16 entries once exploded (12 table,
2 sequence, 2 function, for `anon` and `authenticated`). `ALTER DEFAULT PRIVILEGES ... REVOKE`
without `FOR ROLE` only touches the **current** role's defaults, so 0001 could not see them.

**The first attempt was to revoke them under each grantor `pg_default_acl` names.** That is
impossible on the real thing, and CI said so in as many words (run `32122434872`):

```
ERROR: 0001: cannot clear the default privileges held by grantor supabase_admin in schema public
       (object type S). The role applying this migration is postgres, which is not a member of
       supabase_admin, so ALTER DEFAULT PRIVILEGES FOR ROLE is refused.
```

**So the assert itself was wrong — over-broad — and this is the argued case, not a quiet
softening.** The governing fact:

> **A `pg_default_acl` row applies ONLY to objects created by its own grantor.** It is not a
> schema-wide rule.

Measured on PostgreSQL 18.3, with all 16 entries in place and 0001 §5a applied:

| created by | resulting ACL |
|---|---|
| the migration role (`postgres`) | `relacl = null` — **no client privilege at all** |
| `supabase_admin` | `anon` and `authenticated` get INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, SELECT |

Same split for sequences and for functions. So `supabase_admin`'s defaults cannot reach a single
object this game owns, and the old assert demanded control over something the migration cannot
control **and** that does not threaten it — it would have blocked every deploy, forever. An assert
that can never pass is not strictness; it is pressure to delete the check.

**0001 now asserts what it can actually guarantee:**

| | |
|---|---|
| **(d)** | default ACLs **owned by the role applying the chain** grant no client a write or execute. Kept, and narrowed to this — these are the ones that govern every object the chain creates. |
| **(d2)** | default ACLs owned by **any other** grantor are printed as a permanent `NOTICE` on **every apply**, naming the grantor and the object types. Never swallowed, never fatal. |
| **(b)(c)** | no **current** table carries a client write, through `client_write_grants()` and independently through `information_schema`. Unchanged; proof 03 confirms it by actually being `anon`. |
| **(i)** | **NEW.** Every table, sequence, view and function in all four schemas is **owned by the role applying the chain** — so none of them can have inherited a foreign grantor's defaults. This is what converts (d2)'s un-revokable entries from an unprovable claim into an **irrelevant** one. |

(i) has a positive control that needs no privileges: the same authority,
`public.objects_not_owned_by()`, is asked about a role that owns nothing and must return rows,
which proves the scan reaches these schemas and that the owner comparison discriminates. Extension
members are excluded — their owner is the platform's choice, not ours — and the count skipped is
NOTICEd rather than hidden.

**§5b deliberately does not attempt the foreign revoke at all**, not even opportunistically. It
would succeed locally (the harness runs as a superuser) and fail on Supabase, putting the cheap gate
and CI back on different code paths — which is the original defect in this whole episode.

`objects_not_owned_by()` is also asserted at **end of chain** by proof 03's eighth marker,
`GRANT_LOCKDOWN_CHAIN_OWNS_EVERYTHING`, which CI re-runs against the disposable Supabase; and the
workflow re-checks both claims from raw catalogue queries, independently of the chain's own
authorities.

**Nothing was deployed anywhere, so 0001 was amended in place; no 0011 patch.**

**And the local gate was made able to see any of this.** `npm run db:apply` boots a bare PGlite,
which has no client roles and no default ACLs at all — so this assert, and the whole grant family
with it, passed **vacuously**: nothing to find. `scripts/db/apply-chain.mjs` now applies
`scripts/db/supabase-preamble.sql` first (a **test fixture, never a migration**), installing those
roles and those 16 default privileges under a foreign grantor. With it in place the original 0001
failed locally with the identical 16-entry message CI reported, and the current 0001 prints the
same `NOTE` locally that it will print on Supabase.

---

## What is NOT proven here

* **Supabase's own roles.** The preamble is a fixture a human wrote from what CI reported; it can
  drift from the platform, and it models only roles and default ACLs — not that the migration role
  is a **non-superuser** there, which is precisely what made the first fix impossible. Only the
  disposable-Supabase job in `.github/workflows/migrations-apply-proof.yml` meets the real roles,
  and it now re-checks the default ACLs and the object ownership from outside the migration.
* **That the un-revokable platform defaults stay harmless.** They are harmless *because* nothing
  here is created by `supabase_admin`. If a future migration ever runs `set role`, or an extension
  is installed into one of these four schemas by another role, that stops being true — which is why
  assert (i) and proof 03's `GRANT_LOCKDOWN_CHAIN_OWNS_EVERYTHING` run on every apply rather than
  being a one-time argument written in a comment.
* **pg_cron.** The tick functions are proven by being called, not by being scheduled. Nothing here
  shows a cron entry firing on a real Supabase project.
* **PostgREST exposure.** The RPCs live in the `world` / `cmd` / `voyage` schemas, as DESIGN
  Appendix 2 names them. Supabase's PostgREST exposes `public` by default: the project's API
  settings need `world, cmd, voyage` added to the exposed schemas before a browser can call them.
  That is configuration, not a second entry point, and it is deliberately not worked around with
  duplicate `public.` wrappers.
